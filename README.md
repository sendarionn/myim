# myim

複数のローカル辞書と拡張辞書を使うmacOS向けInput Method

POBox、Gyaim、SKKにインスピレーションを受けて制作しました

## 設計思想

myimは、日本語入力と英語入力を別のモードへ分けず、ローマ字から必要な表記を選ぶInput Methodです

- 変換前はローマ字を保ち、候補を選んだ時だけ日本語や別の表記へ変換
- 辞書の基本単位を`input → candidates`の単純な組にし、日本語変換、略語、英語、固有名詞を同じ形式で扱う
- 基本辞書、ユーザー辞書、共有可能な拡張辞書を重ね、個人の語彙と共同体の語彙を両立
- 辞書登録を別アプリの作業にせず、普段の入力から連続して実行
- 選択履歴、次入力学習、動詞活用、誤入力補完、外部情報を交換可能な補助層として追加
- ローカル変換を基本とし、通信を伴う機能は任意で有効化
- 候補に対応するCosenseページや辞書をそのまま表示し、語の意味や背景へ入力中に接続

複雑な自然言語処理を前提にするのではなく、単純な辞書検索を中心に、必要な能力を独立して重ねられる構成を目指します

## 必要環境

- macOS 13以降
- Apple Translationを使う機能はmacOS 15以降

## 基本操作

入力中はローマ字をそのまま表示し、候補を選択した時点で日本語へ変換します

| 操作 | 結果 |
| --- | --- |
| 文字入力 | 候補をインクリメンタルに更新 |
| Tab | 先頭候補を選択し、続けて押すと次候補へ移動 |
| Shift＋Tab | 「もしかして？」候補を選択 |
| 左右矢印 | 候補未選択なら入力カーソルを移動、候補選択後は候補を移動 |
| 上下矢印 | 候補選択後に候補を移動 |
| Return | 選択候補を確定、未選択ならローマ字のまま確定 |
| Space | 入力または選択候補と半角空白を確定し、次入力候補の表示を継続 |
| Shift＋Space | 入力または選択候補と全角スペースを確定 |
| Escape | 候補選択を解除してローマ字表示へ戻る |
| Delete | 入力を削除して候補を更新 |
| Option＋Delete | 直前の単語単位を削除して候補を更新 |
| Command＋Delete | 未確定文字列全体を削除して候補を閉じる |
| Command＋O | 外部情報パネルのページを既定ブラウザで開く |
| Option＋T | 翻訳モードをオンまたはオフにする |
| F6 | 入力をひらがな候補へ直接変換 |
| F7 | 入力を全角カタカナ候補へ直接変換 |
| F8 | 入力を半角カタカナ候補へ直接変換 |
| F9 | 入力を全角英数字候補へ直接変換 |
| F10 | 入力を半角英数字候補へ直接変換 |

F6〜F10は入力位置の未確定範囲だけを変換し、候補パネルや外部情報パネルへフォーカスを移しません

候補パネルのショートカット案内は、パネル幅に合わせて複数行へ折り返します

macOSのSecure Event Inputを検知すると、未確定入力と各パネルを閉じてmyimの処理を一時停止します
保護入力中のキーは加工せず入力先へ渡し、候補生成、履歴保存、外部通信を行いません
Secure Event Inputが終了すると、次の入力から通常動作へ自動的に戻ります
検知状態と直前の検知結果は入力ソースメニューの「状態を確認…」で確認できます

入力可能な場所で確定済みの文字列を選択してTabを押すと、辞書から読みを逆引きして再変換を開始します
Escapeまたはフォーカス移動では元の文字列へ戻します
閲覧専用の文字列など、Tabがmyimへ届かない場所では利用できません

候補パネルは入力位置から少し間隔を空け、画面内に最大7件を表示します

パネル下部には現在の状態で使える主要なショートカットを表示します

Tabを続けて押すと8件目以降も選択できます

辞書に候補がない場合も、ひらがなと片仮名を表示します

記号入力、表記揺れ、連濁、候補順、動詞活用の詳細は`MISCELLANEOUS.md`を参照してください

## 主な機能

### ローカル変換

- Mozc OSS辞書を主辞書として一般的なかな漢字変換を提供
- TKG Japanese-English Learner’s Dictionaryで日本語語彙を補完
- 終止形から動詞のて形、た形、否定形、丁寧形、可能形、受身形、使役形などを生成
- macOSの英語補完から英単語候補を表示
- 1文字分の入力ではひらがな・片仮名を最優先
- 2文字分以上では完全一致する直接候補、直近に選択した候補、かな候補、その他の順で表示

同梱辞書はMozcとTKGJEの原本ではなく、myimに必要な読みと候補を抽出した派生データです

Mozcのかな読みは同梱前に正規ローマ字inputへ変換します
`si / shi`などの正しい入力表記差は検索前のローマ字解析で吸収し、タイポは「もしかして？」で別に処理します

辞書の構成と変換方法は`docs/DICTIONARY_DATASETS.md`、出典と配布条件は`docs/DICTIONARY_LICENSES.md`を参照してください

### 便利機能

- 計算: `2+1=`から`3`を候補として表示
- 文字種変換: F6〜F10でひらがな、カタカナ、英数字へ直接変換
- 日付: `kyou` `kinou` `ashita`から対象日を候補として表示
- 時刻: `ima` `jikoku`から現在時刻を候補として表示
- JavaScript拡張: 入力から動的な候補を生成

計算は四則演算、小数、括弧に対応し、式の末尾が`=`の場合だけ実行します

日付と時刻は設定の「日時の動的候補を表示」を有効にすると利用できます
書式は`YYYYMMDD` `YYYY-MM-DD` `HH:mm:ss`などをカンマ区切りで指定できます

### JavaScript拡張

次のフォルダへ`.js`ファイルを追加すると、入力に応じた候補を生成できます

```text
~/Library/Application Support/myim/Extensions/
```

このフォルダは、myimがユーザーごとのJavaScript拡張を保存する場所です

- macOSの非表示`Library`内にあり、入力ソースメニューの「JavaScript拡張フォルダを開く…」からFinderで表示
- myimを初めて起動するか、メニューからフォルダを開いた時に自動作成
- 標準の`datetime.js`も初回作成時に同じフォルダへ配置
- `nendo.js`は4月始まりの現在年度を西暦と和暦で生成
- `gengou.js`は現在の元号年を漢字表記と略号で生成
- ユーザーが追加した`.js`と標準拡張を同じ仕組みで実行
- myimの再インストールや更新では、配置済みのファイルを上書きしない
- フォルダ直下の`.js`だけを拡張として読み込み、サブフォルダは読み込まない

```javascript
// @myim-prefix hello

function candidates(context) {
  if (context.input === "hello") {
    return ["Hello", "こんにちは"]
  }
  return []
}
```

ファイルの追加、変更、削除は次の入力から反映します
`@myim-prefix`を指定すると、その文字列で始まる入力だけで拡張を実行します

入力ソースメニューを開き、「myim設定…」直下の「JavaScript拡張フォルダを開く…」を押すと配置先をFinderで表示します

「JavaScript拡張を管理…」では、各`.js`のファイル名、prefix、実行状態を確認できます
チェックを外すとその拡張だけを無効化し、設定は再起動後も維持します
「再読み込み」は補助プロセスと状態表示をリセットし、次の入力からファイルを読み直します
構文エラーは対象ファイルの行に、タイムアウトは管理画面の上部に表示します

`context`には`input`、`timestamp`、`timeZone`、日時書式設定を渡します
候補は文字列、または`value`を持つオブジェクトの配列で返します

標準の日時候補も同じAPIを使う`datetime.js`として同梱しています
`nendo`では`2026年度`と`令和8年度`、`gengou`では`令和8年`と`R8`を生成します
初回利用時に`datetime.js`、`nendo.js`、`gengou.js`をユーザー拡張フォルダへ配置します
配置後の編集や削除は再インストール時にも上書きしません
既存のSwift版と結果を比較できる移行期間中は、重複を候補統合時に除去します

JavaScriptはネットワーク、ファイル、シェルへ接続するAPIを持たない補助プロセスで実行します
応答しない拡張は100ミリ秒で補助プロセスごと停止し、myim本体での入力を継続します

### 「もしかして？」候補

誤入力の補助候補を、通常候補とは別の「もしかして？」パネルへ表示します

- 削除索引とローマ字向け重み付き編集距離で誤入力を補完
- 隣接キー、母音の脱落、文字の入れ替え、長音、`n`表記を優先
- 誤入力コストと基本辞書順を組み合わせて候補を順位付け
- 過去に選択した補助候補を直近の選択順で優先
- Shift＋Tabで選択を開始
- Tabと矢印キーで移動
- Returnで確定
- Escapeで通常候補へ戻る
- 通常候補の選択をEscapeで解除すると「もしかして？」候補も再表示

### ユーザー辞書

入力中のローマ字を読みとして、任意の文字列を登録できます

1. myimで読みを入力
2. Option＋Returnで辞書登録モードへ移る
3. 登録したい文字列を入力するかCommand＋Vで貼り付ける
4. Returnで入力を追加し、最後にもう一度Returnで登録を確定

通常入力中のCommand＋Vは辞書登録を開始せず、入力先アプリへ通常の貼り付けとして渡します

辞書登録中は各パネルにアクセント色の枠と「登録したい文字列を入力」の見出しを表示します

登録した候補を選択してCommand＋Xを押すと、入力中の読みが完全一致していなくてもユーザー辞書から削除します
同じ候補が基本辞書などにも存在する場合、基本辞書側の候補は削除しません
削除した文字列は誤削除に備えてクリップボードへコピーします

保存先

```text
~/Library/Application Support/myim/user/dictionary.txt
```

### 次の入力候補

確定した語句のつながりをローカルで学習し、次に入力される可能性が高い語句を表示します

同じ語句の後で直前に選んだ候補を先頭にし、2番目以降は累計頻度と最終使用順で表示します

- Tabで選択を開始
- Tabと矢印キーで移動
- Returnで確定
- Escapeで候補パネルを閉じ、入力欄のフォーカスを維持
- 文字入力を始めると選択中の候補を確定して新しい入力へ移行
- 未選択のまま5秒経過すると自動で終了
- Spaceを入力しても次入力候補パネルを維持
- 最大256文脈、1文脈につき最大8候補だけを保持

入力ソースメニューの「次入力履歴を削除」から学習内容を削除できます
次入力履歴は通常候補の順序にも使用し、直前の確定語に続いて選ばれた候補を優先します

### 辞書と外部情報の表示

候補選択中に次の補助パネルを表示できます

- macOS標準辞書の語義を候補パネルの上または下へ表示
- CosenseやWikipediaなどのWebページを候補パネルの左または右へ表示
- 「もしかして？」パネルを入力行と候補パネルに重ならない位置へ表示
- Webパネル内のクリックとスクロールに対応
- タイトルバーの「ブラウザで開く  ⌘O」またはCommand＋Oで現在のページを既定ブラウザへ表示
- パネル操作後も未確定文字列と候補選択を維持
- Escapeまたは次のキー入力で候補選択へ戻る

候補選択前は入力中のローマ字をmacOS標準辞書で検索し、入力に合わせて語義を更新します

候補選択へ移ると、選択中の候補を検索対象に切り替えます

外部情報パネルは`%s`を検索語として含む任意のHTTPまたはHTTPS URLを指定できます

表示までの待機時間も設定できます

macOS標準辞書の利用範囲は`docs/MACOS_DICTIONARY.md`を参照してください

### 外部候補とWeb検索

必要な機能だけを設定から有効にできます

- Wikipediaを辞書として利用
- Apple Translationによる日本語から英語への候補
- Command＋Returnで選択候補を確定してWeb検索

Web検索URLは`https://www.google.com/search?q=%s`のように指定します

Web検索URLは「Web検索先を設定…」から変更できます

Wikipedia、Apple Translation、Web検索は初期状態で無効です

TKGJE基本辞書の自動更新確認はプロセス全体で共有し、6時間に1回実行します
「TKGJE基本辞書を更新」では待機時間に関係なく手動確認できます

### 翻訳モード

macOS 15以降では、入力ソースメニューから翻訳モードを有効にできます
Option＋Tでも翻訳モードを切り替えられます
切り替えると入力位置付近に現在のオン・オフ状態を短時間表示します

1. 通常どおり日本語を変換してReturn
2. 日本語を本文へ確定せず「翻訳する日本語」パネルへ追加
3. 必要なら続きの日本語を入力してReturnで追加
4. 入力が空の状態でもう一度Return
5. Apple Translationで英訳し、成功すると英語だけを本文へ確定

翻訳前の日本語は未確定文字列として入力位置へ表示し、辞書登録モードと同様のパネルで状態と操作を案内します
翻訳を実行するReturnは改行として入力先へ送りません
Escapeでは翻訳せず、保持中の日本語をそのまま本文へ確定します
翻訳下書きではSpaceと連続Spaceを空白として保持します
入力途中で翻訳モードをオフにすると、保持中の日本語と現在の入力を日本語のまま確定します

通常の「日本語入力から英語の変換候補を取得」は単語向けとして扱い、翻訳結果の先頭にある`a` `an` `the`と末尾のピリオドを候補表示から除きます

Apple Translationは翻訳スタイルを指定できないため、通常候補が必ず辞書の見出し語と一致するわけではありません

### Cosense拡張辞書

指定したCosenseプロジェクトの`dictionary`ページを共有語彙や固有名詞の拡張辞書として使用します

```text
https://scrapbox.io/project-name
```

ページの記述形式

```text
miru
 見る
 診る

ikiru
 生きる
 活きる
```

- 入力ソースメニューの「Cosenseプロジェクトを設定…」からURLを指定
- 設定直後に同期し、プロジェクト別にローカルへキャッシュ
- 「Cosense辞書を更新」またはCommand＋Option＋Control＋Rで再同期
- 公開プロジェクトとログイン済みのプライベートプロジェクトに対応
- 外部情報パネルは汎用ブラウザ補助プロセスで表示
- クリック、スクロール、ログイン欄への文字入力が可能
- CookieなどのWebサイトデータは補助プロセスの永続WebKitデータストアだけで管理
- Cosenseへのユーザー辞書の書き戻しは行わない

## 設定

入力ソースメニューの「myim設定…」から開きます

コア機能はローマ字入力とローカル候補生成です

それ以外は個別にオン・オフできます

- 常時使用するローカル基本辞書とユーザー辞書
- オン・オフできるCosense拡張辞書
- 英語補完と外部候補
- 誤入力補完
- 次入力候補
- macOS辞書パネルと外部情報パネル
- Web検索と検索先URL

設定変更は現在の入力から反映し、myimの再起動後も維持します

## ビルドとインストール

### GitHub Releaseからインストール

1. GitHub Releaseから最新版の`myim-バージョン.pkg`をダウンロード
2. PKGを開いてインストール
3. 「システム設定」→「キーボード」→「テキスト入力」→「編集」を開く
4. `myim`を追加

PKGは`/Library/Input Methods/myim.app`へインストールします

### ソースからインストール

Swift 6以降が必要です

```shell
./Scripts/install-macos-im.sh
```

初回だけ「システム設定」→「キーボード」→「テキスト入力」→「編集」から`myim`を追加します

更新時も同じコマンドで最新コードをビルドして置き換えます

登録済みの入力ソースは維持し、更新時にシステム設定を開いたり追加許可を求めたりしません

インストール先

```text
~/Library/Input Methods/myim.app
```

ログアウトや再起動は不要です

## テスト

```shell
swift test
python3 -m unittest discover -s Tests/ScriptTests -p 'test_*.py'
```

## クラッシュログ

myimが終了して入力できなくなった場合は、次の場所から最新の`myim`クラッシュログを取得します

1. 「コンソール」アプリを開く
2. サイドバーの「クラッシュレポート」を開く
3. プロセス名が`myim`の最新レポートを選ぶ
4. レポートを保存する

ターミナルでは次のコマンドで候補を確認できます

```shell
ls -lt ~/Library/Logs/DiagnosticReports/myim*.ips
```

入力を一時的に復旧する場合は、別の入力ソースへ切り替えてからmyimへ戻します
復旧しない場合はログアウトして再ログインします

## 実装予定

### 機能追加

- 辞書登録と外部情報パネルのInputMethodKit実機テストを拡充
- カーソル位置の入力モードインジケータをライト・ダーク両モードで安定化
- 変換エンジンをmacOS固有実装から分離してWindowsへ対応
- 権限と表示領域を定義した拡張フォルダ形式

### リファクタリング

- 基本辞書、ユーザー辞書、Cosense辞書の読み込み、更新、キャッシュ、変換エンジン再構築を共通管理
- ユーザー辞書の登録、削除、保存、候補更新を入力制御から分離
- Wikipedia、翻訳、英語補完、Web検索の外部候補処理を共通管理
- 各パネルの配置、表示、フォーカス復帰を共通管理
- UserDefaultsの設定キー、初期値、読み書きを型付き設定へ集約
- `InputController`に残る候補選択と確定を`MyIMECore`へ移動
- Cosense、外部候補、誤入力補完、次入力予測、外部情報表示を拡張機能として分離
- 通常候補だけでなく拡張機能の候補も`CandidatePipeline`で順位付け
- JavaScript実行エンジンを抽象化し、Windows版では交換可能にする
- 必要性と安全性を確認した機能だけ、将来XPCなどの外部プラグイン方式を検討

## 関連資料

- `HISTORY.md` 実装の更新履歴
- `MISCELLANEOUS.md` 入力変換と候補順の細かな調整
- `docs/DICTIONARY_DATASETS.md` 辞書の生成方法と実行時構成
- `docs/DICTIONARY_LICENSES.md` 辞書の出典とライセンス
- `docs/MACOS_DICTIONARY.md` macOS標準辞書の利用方針
- `docs/RELEASING.md` 署名、公証、GitHub Releaseの手順
