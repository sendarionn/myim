# 細かな入力調整

## 候補生成の負荷

- Mozc辞書は全行をオブジェクト化せず、メモリマップと読み位置索引で検索
- 読みをUTF-8バイト列で二分探索し、該当候補だけを復号
- その他のローカル辞書も読みを整列し、接頭辞を二分探索
- 履歴がある候補だけを直近選択順で並べ替え
- 選択履歴は直近4,096候補だけを保持
- 履歴がない候補は辞書内の順序を維持し、比較ソートを省略
- 完全一致候補と前方一致候補の重複判定にはSetを使用
- 全候補を内部に保持し、8件目以降へ移動できる動作を維持

## 記号入力

ASCII記号から日本語の句読点、括弧、長音などを候補として表示します

対応する全角記号、日本語組版向け記号を同じ入力から選択できます

入力した半角記号そのものは候補へ重複表示せず、候補未選択のままReturnで確定できます

例

- `!` → `！`
- `?` → `？`
- `(` → `（`、`「`、`『`
- `-` → `－`、`ー`、`―`、`−`、`—`
- `~` → `〜`、`～`
- `=` → `＝`、`≒`、`≠`
- `*` → `＊`、`※`

記号候補には通常の選択履歴を適用しません

対応する全角記号、日本語組版向けの関連記号の順を維持します

次の`z`入力を記号へ変換します

- `zh` → `←`
- `zj` → `↓`
- `zk` → `↑`
- `zl` → `→`

## 数字入力

ASCII数字の`0`〜`20`から丸数字、全角数字、漢数字を候補として表示します

例

- `1` → `①`、`１`、`一`、`壱`
- `12` → `⑫`、`１２`、`十二`
- `20` → `⑳`、`２０`、`二十`

入力した半角数字そのものは候補へ重複表示せず、候補未選択のままReturnで確定できます
- `z-` → `〜`
- `z[` → `『`
- `z]` → `』`
- `z,` → `‥`
- `z.` → `…`
- `z/` → `・`

## 拡張ローマ字

- `thi` → `てぃ`
- `cha-to`の`-`を長音として扱い、`チャート`などを検索
- 小書き文字は`x`または`l`から入力
- `xtsu`、`xtu`、`ltsu`、`ltu` → `っ`

## 入力表記の正規化

未確定文字列には入力した大文字と小文字をそのまま表示します

辞書検索時だけ小文字化し、次の表記差をTKGJEの読みに統一します

- `tu`と`tsu`
- `hu`と`fu`
- `du`と`zu`
- `si`と`shi`
- `ti`と`chi`
- `zi`と`ji`
- `sya`と`sha`
- `tya`と`cha`
- `zya`と`ja`

例

- `tuujou`、`tuuzyou`、`tsuujou`から`通常`
- `hukumu`、`fukumu`から`含む`
- `syuusei`、`shuusei`から`修正`
- `hiduke`、`hizuke`から`日付`
- `cha-to`を`chaato`として検索し`チャート`

Shiftで大文字にした英字は、英語補完候補を選択して確定した後も維持します

例

- `Ret` → `Return`
- `RET` → `RETurn`

## 候補の生成順

`〜個`や`〜など`のように別の語句が入る位置を`〜`で示した候補は、基本辞書への変換時に`個`や`など`へ正規化します

ユーザー辞書やCosense拡張辞書に同じ表記がある場合も、候補パネルと確定文字列から`〜`を除去します

`〜`だけの記号候補は除去しません

ひらがな変換結果が1文字の場合は、ひらがなと片仮名を最優先で表示します

1. ひらがな
2. 片仮名
3. 入力読みに完全一致する直接候補
4. 選択履歴のある候補を直近順
5. その他の候補

ひらがな変換結果が2文字以上の場合は次の順で表示します

1. 入力読みに完全一致する直接候補
2. 選択履歴のある候補を直近順
3. ひらがな
4. 片仮名
5. その他の候補

直接候補内では、ローカルユーザー辞書、Cosense拡張辞書、Mozc、TKGJE、動詞活用、myim補完辞書の順を維持します

その他の候補では、ユーザー辞書と各辞書の前方一致、英語補完、外部候補を対象に、選択履歴のある候補だけを前へ移動します

同じ候補が複数の候補元にある場合は、先に現れた候補だけを残します

## 連濁の入力揺れ

語頭の濁音または半濁音を清音へ戻した読みでも辞書を検索します

例

- `doori` → `doori`と`toori`を検索 → `どおり`、`通り`
- `gawa` → `gawa`と`kawa`を検索

候補は内部ですべて保持し、候補ウィンドウには選択位置を含む7件だけを表示します

Tabでは候補順に移動します

曖昧検索では元の読みを保持したまま、母音前と末尾の`nn`を単一`n`へ寄せた補助読みも検索します

例: `genninn` / `gennin` → `genin` → `原因`

別の完全一致候補がある場合も、表記正規化後の距離が0になる候補は「もしかして？」へ表示します

同じ長さで1文字の置換または入れ替えに収まる候補も、高信頼の誤タイプ候補として表示します

例: `ainai` → `aimai` → `曖昧`

QWERTY配列で隣接するキーの1文字置換を、入力の長さにかかわらず検索します

隣接キーから作る読みは辞書索引へ直接照合し、読みごとの辞書走査を増やしません

例: `eo` → `wo` → `を`

文字数が変わる距離1候補と距離2以上の候補は、直接候補を邪魔しないよう抑制します

次の補助読みも辞書側と入力側の両方へ適用します

- 撥音の区切り: `gen'in` → `genin`
- 唇音前の撥音: `shimbun` → `shinbun`
- 長音の表記差: `ou/oo/o`、`ei/ee/e`、同一母音の連続
- 既存の訓令式・ヘボン式差: `si/shi`、`tu/tsu`など

補助読みは通常候補へ混在させず、直接一致候補がない場合だけ「もしかして？」パネルで使用します

## 長音記号を含む入力の候補順

`-`で長音を明示した2文字分以上の入力でも、完全一致する辞書候補を優先します

例: `ki-`では完全一致候補の後へ`きー`と`キー`を表示します

直接一致しない活用候補や前方一致候補はその後へ表示します

矢印キーでは可変幅セルの実際の配置に合わせて左、右、上、下へ移動します

## 動詞活用

Mozc OSSまたはTKGJEに収録された終止形から、て形、た形、否定形、丁寧形などの候補を生成します

- `utsutte`から`utsuru`を検索し`移って`
- `yonde`から`yomu`を検索し`読んで`
- `kaite`から`kaku`を検索し`書いて`
- `itte`から`iku`を検索し`行って`
- `kangaete`から`kangaeru`を検索し`考えて`
- `benkyoushite`から`benkyousuru`を検索し`勉強して`
- `mirareru`から`miru`を検索し`見られる`
- `kakeru`から`kaku`を検索し`書ける`
- `yomeru`から`yomu`を検索し`読める`
- `kakaseru`から`kaku`を検索し`書かせる`

一段動詞の可能・受身と使役、五段動詞の可能・受身と使役を終止形から生成します

## 漢字候補を補完する方針

`kakujuu`から`拡充`が出なかった問題は、TKGJEを単独の基本辞書としていたことが原因です

次の構成で補完します

1. Mozc OSSをかな漢字変換の主辞書として保持
2. TKGJEを学習者向け日英補助辞書として保持
3. myim補完辞書を両方から独立したレイヤーとして保持
4. 既存辞書の完全一致より後、かな候補より前へ補完候補を配置
5. `kakujuu` → `拡充`などの欠落例を回帰テストとして蓄積
6. 活用型を持つ公開辞書を追加レイヤーへ変換

追加データはライセンス、更新方法、容量、固有名詞と活用語の収録範囲を比較して決定します

候補がないたびに個別ルールを増やす方式は避け、辞書レイヤーの不足として一括補完します

## 日付と時刻の入力方針

実験機能を有効にすると、日付や時刻を表す読みへ変換時点の値を動的な候補として追加します

- `kyou` → `2026年8月10日` `2026-08-10` `2026/08/10` `20260810`
- `kinou` → 前日を同じ4書式で表示
- `ototoi` `ashita` `asatte` → 対象日を同じ4書式で表示
- `ima` `jikoku` `genzaijikoku` → `16:59` `16:59:12` `16時59分`など
- `nichiji` `genzainichiji` → 日付と時刻を組み合わせた候補

書式は通常の候補選択で切り替えます

設定では日付、時刻、日時の書式をカンマ区切りで個別に指定します

使用できるトークン

- `YYYY` `YY` 年
- `MM` `M` 月
- `DD` `D` 日
- `HH` `H` 時
- `mm` `m` 分
- `ss` `s` 秒

日付計算には利用中のシステムタイムゾーンを使います
