# 細かな入力調整

## 記号入力

ASCII記号から日本語の句読点、括弧、長音などを候補として表示します

次の`z`入力を記号へ変換します

- `zh` → `←`
- `zj` → `↓`
- `zk` → `↑`
- `zl` → `→`
- `z-` → `〜`
- `z[` → `『`
- `z]` → `』`
- `z,` → `‥`
- `z.` → `…`
- `z/` → `・`

## 拡張ローマ字

- `thi` → `てぃ`
- `cha-to`の`-`を長音として扱い、`チャート`などを検索
- 小書き文字は`x`または`l`から入力
- `xtsu`、`xtu`、`ltsu`、`ltu` → `っ`

## 入力表記の正規化

未確定文字列には入力した大文字と小文字をそのまま表示します

辞書検索時だけ小文字化し、次の表記差をTKGJEの読みに統一します

- `tu`と`tsu`
- `hu`と`fu`
- `du`と`zu`
- `si`と`shi`
- `ti`と`chi`
- `zi`と`ji`
- `sya`と`sha`
- `tya`と`cha`
- `zya`と`ja`

例

- `tuujou`、`tuuzyou`、`tsuujou`から`通常`
- `hukumu`、`fukumu`から`含む`
- `syuusei`、`shuusei`から`修正`
- `hiduke`、`hizuke`から`日付`
- `cha-to`を`chaato`として検索し`チャート`

Shiftで大文字にした英字は、英語補完候補を選択して確定した後も維持します

例

- `Ret` → `Return`
- `RET` → `RETurn`

## 候補の生成順

ひらがな変換結果が1文字の場合は次の順で表示します

1. ローカルユーザー辞書
2. Cosense拡張辞書
3. ひらがな
4. 片仮名
5. TKGJEの完全一致候補
6. 動詞活用候補
7. 英語補完
8. TKGJEの前方一致候補

ひらがな変換結果が2文字以上の場合は漢字候補を優先します

1. ローカルユーザー辞書
2. Cosense拡張辞書
3. TKGJEの完全一致候補
4. 動詞活用候補
5. ひらがな
6. 片仮名
7. 英語補完
8. TKGJEの前方一致候補

同じ候補が複数の候補元にある場合は、先に現れた候補だけを残します

候補は内部ですべて保持し、候補ウィンドウには選択位置を含む7件だけを表示します

Spaceでは候補順に移動します

矢印キーでは可変幅セルの実際の配置に合わせて左、右、上、下へ移動します

## 動詞活用

TKGJEに収録された終止形から、て形とた形の候補を生成します

- `utsutte`から`utsuru`を検索し`移って`
- `yonde`から`yomu`を検索し`読んで`
- `kaite`から`kaku`を検索し`書いて`
- `itte`から`iku`を検索し`行って`
