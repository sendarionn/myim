# 開発方針

- 入力文字列と出力書式を定義する機能はJavaScript拡張として実装する
- UI、認証、外部データ取得、OS連携はSwift側で実装する
- 同じ候補生成処理をSwiftとJavaScriptへ重複実装しない
- UIの操作ラベルには、意味のない`...`や`…`を付けない
- 実装前に、要望の本質的な目的を満たせるか確認する
- 認証、配布、外部サービスの制約を含め、継続運用が現実的か確認してから方式を決める
- 技術的に実装可能でも目的を満たさない場合は、実装前に制約と代替案を提示する
