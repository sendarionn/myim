# 実装履歴

## 2026-08-14

### 辞書エンジンのメモリ使用量

- InputControllerごとに重複していた基本辞書、Mozc索引、動詞活用索引を共有
- 同じ辞書内容の誤入力補完索引を再利用し、アプリごとの重複構築を防止
- ユーザー辞書や拡張辞書の変更時に基本辞書索引を再構築しないよう修正
- 誤入力補完索引を連続配列へ変更し、小さな辞書と配列の大量生成を削減
- 本体側に残っていた重複WKWebViewを削除
- 外部情報パネルを閉じて30秒後に補助ブラウザを終了し、WebKitメモリを解放
- 誤入力補完索引の参照を共有リポジトリへ一本化し、古い世代を各入力先が保持しないよう修正
- 同じ辞書内容からの索引再構築要求を共有リポジトリで集約

### 翻訳モードの下書き編集

- 翻訳確定前の日本語をDeleteで末尾から削除できるよう修正
- Option＋DeleteとCommand＋Deleteでも翻訳下書きを単語単位・全体で削除可能に変更
- 下書きを全削除しても翻訳モードを維持
- 翻訳下書きへSpaceと連続Spaceを入力できるよう修正
- 入力途中で翻訳モードをオフにした場合は翻訳前の日本語で確定するよう変更

### 基本辞書の更新負荷

- 入力先ごとに重複していたTKGJEの更新通信とJSON解析をプロセス全体で共有
- 自動更新確認を6時間に1回へ制限
- 設定画面からの手動更新では確認間隔を無視して即時確認

### InputMethodKit接続の復旧

- macOSが拒否していたアンダースコア形式の`InputMethodConnectionName`をドット区切りへ修正
- Info.plistと`IMKServer`で同じ接続名を使用し、入力と入力ソースメニューの接続を復旧

### Wikipedia候補へ復元

- 汎用JSON API辞書、OpenSearch自動検出、Web辞書サイト登録を削除
- 外部候補をWikipedia OpenSearch APIとApple Translationのシンプルな構成へ復元
- Web検索URLの設定と外部情報パネルの任意URL設定は維持
- 設定名を「Wikipediaを辞書として利用」へ変更
- myim設定から翻訳モードのオン・オフを削除し、入力ソースメニューだけに集約

### 設定画面の初期位置

- 設定内容を上から配置するスクロールビューへ変更
- myim設定を開くたびにスクロール位置を先頭へ戻すよう修正

### 入力ソースの更新

- インストーラーは登録済み入力ソースを再登録せず、初回だけ登録と選択を実行
- 更新時のLaunch Services登録解除をやめ、システム設定の許可画面が毎回開く問題を修正
- 登録済みmyimへの`TISEnableInputSource`も省略し、`swift-frontend`による有効化許可を更新時に表示しないよう修正

## 2026-07-26

### コマンドライン版モックアップ

- テスト用テキスト辞書の読み込みを実装
- 読みの完全一致検索を実装
- 候補番号による選択と確定を実装
- 選択候補に対応するCosenseページURLを表示

### Cosense辞書の同期

- `sendarionn-public/dictionary`から辞書を取得
- `swift run my-ime sync`による手動同期を実装
- Application Supportへの辞書キャッシュを実装
- 通信や解析に失敗した場合は既存キャッシュを維持

## 2026-07-27

### macOS版IM

- InputMethodKitを使ったmacOS版IMを追加
- 英字入力、未確定文字列、Space変換、Return確定を実装
- Cosense辞書キャッシュを変換辞書として利用

### 入力ソース登録

- Bundle IdentifierをInputMethodKit向けの形式へ変更
- Input Methodsへのインストールと入力ソース登録を確認
- 表示名とアイコンを修正

### 候補操作

- Spaceによる候補循環を実装
- InputMethodKit標準候補パネルの選択表示に関する問題を調査
- 内部状態と表示を一致させるため独自候補パネルへ移行

### ウィンドウ配置

- 候補パネルを現在の入力位置へ配置
- 選択候補に対応するCosenseページを小窓で表示
- Cosenseウィンドウをディスプレイの表示可能領域内へ制限
- 右側に収まらない場合は候補パネルの左側へ配置

## 2026-07-28

### インクリメンタル候補表示

- プロジェクト上の呼称をIMEからIMへ変更
- リポジトリのディレクトリ名を`my-ime`から`myim`へ変更
- 読みの前方一致検索を実装
- 入力と削除のたびに候補を更新
- 候補パネルに未選択状態を追加
- 候補未選択のReturnではローマ字を確定
- Escapeでは候補選択を解除してローマ字表示へ復帰

### 実行中のCosense辞書更新

- 入力ソースメニューに「Cosense辞書を更新」を追加
- `Command＋Shift＋R`による更新を追加
- 更新した辞書をキャッシュへ保存
- IMを再起動せず実行中の変換エンジンへ反映
- 入力中の場合は新しい辞書で候補を再検索
- 入力ソースメニューに更新状態を表示

### 候補パネルのグリッド表示

- 候補一覧を1列のテーブルから縦横のフローレイアウトへ変更
- 各候補の文字列幅に合わせてセル幅を自動調整
- 短い候補を左から詰めてパネル内へ動的に充填
- 候補量に合わせてパネルの幅と高さを自動調整
- 選択候補が見える位置への自動スクロールを維持
- 入力位置が画面端にある場合はパネル位置を表示可能領域内へ補正
- 入力位置の下に収まらない場合は上側へ配置

### インストール手順の統一

- ビルド後の上書き、入力ソース再登録、IM再起動を行うスクリプトを追加
- READMEを現在の動作とインストール方法だけに再構成

### ローマ字からひらがなへの変換

- 入力したローマ字を保持したまま未確定文字列をひらがなで表示
- 通常の五十音、濁音、半濁音、拗音、促音、撥音を変換
- 入力途中で確定できない子音はローマ字のまま表示
- ひらがなと元のローマ字の両方から辞書候補を検索
- Escape後は元のローマ字表示へ復帰
- 候補未選択のReturnでは元のローマ字を確定

### 入力表示をローマ字へ復帰

- 入力中にひらがなへ変換する仕様を撤回
- 候補未選択中は入力したローマ字をそのまま表示
- Spaceで候補を選択した時点で未確定文字列を候補へ切り替え
- Escapeでは候補選択を解除して元のローマ字表示へ復帰
- 辞書検索はローマ字の読みを使用

### macOS標準辞書の語義表示

- Dictionary Servicesの公開APIを使用
- 候補選択時にmacOSで有効な辞書から語義を取得
- 語義とCosenseページを同じ小窓へ表示
- 語義が見つからない場合はその状態を表示
- 標準辞書を変換候補へ利用できる範囲を文書化

### 複数辞書の語義表示

- macOSに搭載された辞書一覧から指定辞書を選別
- スーパー大辞林を個別に検索
- ウィズダム英和辞典・ウィズダム和英辞典を個別に検索
- New Oxford American Dictionaryを個別に検索
- 見つかった語義を辞書名付きで1つのスクロール領域へ表示
- 辞書選別に非公開宣言のシステム関数を使用する互換性リスクを文書化

### 外部辞書データの検討

- 厳密なパブリックドメイン相当とオープンライセンスを分けて調査
- CC0のTKG Japanese-English Learner’s Dictionaryを基本辞書の第一候補に選定
- 実データ29,973項目をmyim形式へ変換
- 変換結果は27,582読み、29,948候補、約606KB
- TKGJE索引をmyim辞書形式へ変換するスクリプトを追加
- UniDic、JMdict、英語公開データのライセンスと用途を文書化
- Cosenseを個人辞書、外部データを基本辞書とする多層構成を今後実装予定

### 辞書更新ショートカット

- ブラウザの再読み込み操作との競合を回避
- `Command＋Shift＋R`から`Command＋Option＋Control＋R`へ変更

### TKGJE基本辞書の採用

- TKGJEから生成した27,582読み、29,948候補をアプリへ同梱
- 基本辞書をローカルだけで読み込める構成へ変更
- 候補表示を最大100件へ制限
- Cosense辞書を基本辞書より優先する拡張レイヤーへ変更
- 同じ読みと候補の重複をレイヤー間で統合

### Cosense拡張辞書の設定

- 入力ソースメニューに「Cosenseプロジェクトを設定…」を追加
- `https://scrapbox.io/project-name`形式のURLを設定
- 指定プロジェクトの`dictionary`ページを拡張辞書として使用
- プロジェクトURLをUserDefaultsへ保存
- プロジェクトごとに拡張辞書キャッシュを分離
- URL設定直後に拡張辞書を自動同期

### アプリ名をmyimへ統一

- アプリ表示名を`my-ime`から`myim`へ変更
- アプリバンドルを`myim.app`へ変更
- 実行ファイルとSwift Packageの実行コマンドを`myim`へ変更
- インストール時に旧`my-ime.app`の登録とファイルを削除
- Application Supportの保存先を`myim`へ変更
- 旧`my-ime`のCosenseキャッシュは移行中も読み込み可能

### TKGJE基本辞書の自動更新

- 起動時にTKGJEの公開索引を確認
- 2026年7月28日12:34 UTC版の29,993項目へ同梱辞書を更新
- 同梱辞書を27,602読み、29,968候補へ更新
- 索引の生成日時が変わった場合だけ基本辞書を更新
- 更新した基本辞書をApplication Supportへ保存
- 更新後の候補へ即時反映
- 入力ソースメニューに手動更新項目と更新状態を追加
- 更新確認に失敗した場合は既存の基本辞書を維持

### Cosense未作成ページの非表示

- 候補ページをCosense APIで事前確認
- HTTP 404などでページが存在しない場合はプレビューを表示しない
- 候補を連続移動した場合は古い存在確認結果を破棄

### 記号を含む入力

- 入力中のASCII記号を未確定文字列へ保持
- 記号より前のローマ字を使って候補検索を継続
- 候補選択時は変換結果の後ろへ記号を保持

### 語義表示とローマ字入力の修正

- Cosenseページの存在判定とOS辞書の語義表示を分離
- CosenseページがなくてもOS辞書の語義があれば小窓を表示
- `cha-to`のハイフンを直前母音の長音として`chaato`へ正規化
- `cha-to`から`チャート`を辞書検索可能に変更
- 拡張ローマ字をひらがなへ変換する候補生成を追加
- `thi`から`てぃ`を候補として生成
- 変換候補の表示上限を7件へ変更

### 候補確定とフォールバック候補

- 候補選択中の次入力で選択候補を確定
- 確定後の文字を新しい未確定入力として処理
- 辞書候補が0件の場合だけひらがな候補を表示
- ひらがな候補と同時に片仮名候補を表示
- ASCII記号から日本語の句読点、括弧、長音、全角記号を生成
- 記号だけの入力から候補選択を開始可能に変更

### 全候補の選択と辞書読み込みの修正

- 変換エンジンでは候補数を制限せず全件検索
- 候補ウィンドウは選択位置を含む7件だけ表示
- Spaceで8件目以降へ移動した場合は表示する7件を切り替え
- ひらがなと片仮名を辞書候補の有無にかかわらず追加
- 完全一致候補を優先しながら、かな候補を最初の7件へ配置
- Shift入力の大文字を保持し、辞書検索時だけ小文字へ正規化
- 複数文字候補に含まれる`〜`と`～`を除いて確定
- 同梱辞書を入力先アプリの`Bundle.main`ではなくmyim自身のバンドルから読み込み
- `tsuujou → 通常`と`fukumu → 含む`を含む基本辞書候補を復旧

### 候補移動と辞書パネルの分離

- 左・上矢印で前候補へ移動
- 右・下矢印で次候補へ移動
- ひらがな変換結果が1文字の場合はCosense候補の直後へ配置
- 同梱辞書をBundle ID、実行ファイル、ユーザー用と全ユーザー用のインストール先から探索
- リソース探索失敗時のログを追加
- メニューのTKGJE更新状態へ読み込み済みの読み数を表示
- 同梱辞書が空の場合は同じ版の取得データでも変換エンジンへ反映

### 辞書パネルの分離

- macOS辞書とCosenseを別々のパネルへ分離
- macOS辞書パネルを候補パネルの上または下へ配置
- Cosenseパネルを候補パネルの左または右へ配置

## 2026-07-29

### 未実装機能の整理

#### 1. Cosenseパネルの操作

- パネルを操作可能にしてクリック、スクロール、リンク移動へ対応
- 表示中のCosenseページを既定のブラウザで開き直す操作を追加

#### 2. ユーザー辞書への登録

- 入力中の`Command＋V`で登録操作を開始
- 登録内容を実行中の拡張辞書へ即時反映
- Cosenseの`dictionary`ページへ登録内容を反映
- Cosenseへの書き込み失敗、競合、認証エラーを表示
- Personal Access TokenまたはService Accountでプライベートプロジェクトを参照
- 認証情報は権限`0600`のユーザー用ファイルへ保存
- 認証済みの読み込みと書き込みで同じ資格情報を使用

#### 3. 日本語から英語への変換

- 日本語の読みから英単語と英語表現を候補として生成
- 日本語候補と英語候補の優先順位を定義
- 英語候補でもOS辞書の語義とCosenseページを表示

#### 4. Windows対応

- macOS固有処理から変換エンジンと辞書管理を分離
- Windows Text Services Framework向けのInput Methodを実装
- 候補、OS辞書相当情報、Cosenseパネルの機能差を整理

英字入力からの英語前方一致候補、多層辞書、TKGJE更新、Cosenseプロジェクト設定は実装済み

### 辞書読みの表記揺れと候補順

- 訓令式とヘボン式のローマ字表記を辞書検索時に正規化
- `tuujou`をTKGJEの`tsuujou`として検索
- `hukumu`をTKGJEの`fukumu`として検索
- Cosense拡張辞書と基本辞書を別々の変換エンジンで検索
- 候補順を入力長と候補元に応じて調整
- レイヤー間で重複する候補は先に現れた候補だけを表示

### 候補優先度と空間移動の調整

- ひらがな1文字ではCosense、ひらがな、片仮名、基本辞書の順に表示
- ひらがな2文字以上ではCosense、基本辞書完全一致、かな候補の順に表示
- TKGJEの完全一致候補と前方一致候補を分離
- macOSの英語スペル補完を候補へ追加
- `ret`などの入力で`return`などの英語候補を表示
- `syuusei`をTKGJEの`shuusei`へ正規化
- `shuusei`と`syuusei`から`修正`を検索
- 左右キーは同じ表示行の隣接セルへ移動
- 上下キーは上下の表示行で横位置が近いセルへ移動
- 移動先が表示中の7件にない場合は前後の候補ページへ移動
- READMEへ候補優先度、全件保持、表記正規化の詳細を追加
- `shuusei`内の`hu`を誤って`fu`へ変換する正規化不具合を修正
- `shu`、`chu`、`thu`、`dhu`を保持したまま単独音節の`hu`だけを`fu`へ正規化

### 辞書読み正規化の共通化

- `hiduke`をTKGJEの`hizuke`へ正規化
- `hiduke`から`日付`を検索
- 入力読みと基本辞書、拡張辞書の読みを同じ正規化器へ通す構成へ変更
- 表記揺れ規則を`RomanizedReadingNormalizer`へ集約

### HISTORYの日付検証

- 2026年7月29日の変更を2026年7月28日の節から分離
- 日本時間の実際の日付を使用
- ビルド時にHISTORYの最新日付が日本時間の当日か検証
- 日付が一致しない場合はビルドを停止

### アプリアイコン

- メニューバーと入力ソース一覧のアイコンを小文字`m`へ変更
- PDFと各サイズのICNSを同じ`m`から生成
- 入力モード専用のメニューアイコンとパレットアイコンを追加
- カーソル位置の入力モードインジケータにも小文字`m`を表示
- アイコンキャッシュ更新のためファイル名とバンドルバージョンを更新
- カーソル位置のチップ向けに`TISIconLabels`の小文字`m`を設定
- 選択中、パレット、通常表示の各アイコン参照を明示
- Apple標準IMと同じ`TISIconLabels.CustomIcon`方式へ変更
- カーソルチップ専用の28×36 PDFアイコンを追加
- `CustomIcon`のPDF領域全体が白いマスクになる問題を確認
- `CustomIcon`を廃止し、macOSが正常にチップ化できる入力モードTIFFへ統一

### 入力ソース切替時のSpace

- Input Methodの有効化時刻を記録
- 切替直後または修飾キー付きの空入力Spaceを消費
- 通常入力中のSpaceによる候補選択は維持

### 動詞活用候補

- TKGJEの終止形からて形とた形を逆引きする仕組みを追加
- `utsutte → utsuru → 移って`へ対応
- `yonde → yomu → 読んで`へ対応
- `kaite → kaku → 書いて`へ対応
- `itte → iku → 行って`の例外へ対応
- 活用候補を基本辞書の完全一致候補の直後へ配置

### ローカルユーザー辞書登録

- 入力中の`Command＋V`で登録操作を開始
- 入力中のローマ字を読みとして使用
- クリップボードの文字列を候補として使用
- 確認アラートを表示せずローカルユーザー辞書へ即時保存
- 登録後はクリップボードの文字列をそのまま変換結果として確定
- 登録内容を実行中の候補へ即時反映
- ローカルユーザー辞書を候補の最優先レイヤーへ追加
- Cosenseへの書き込みは次の段階として継続

### 記号入力と英字表記

- `zl`から`→`などを入力する`z`記号変換を追加
- `zh`、`zj`、`zk`、`zl`による矢印入力へ対応
- Shiftで入力した英字の大文字を英語補完候補の確定後も維持
- 細かな入力調整をREADMEから`MISCELLANEOUS.md`へ移動
- macOSのカーソル位置チップの表示不具合は保留

### プライベートCosenseプロジェクト

- Personal Access TokenとService Accountの認証ヘッダーへ対応
- 認証情報をmacOSキーチェーンへ保存
- Service Accountをプロジェクト単位で管理
- 認証付きで`dictionary`ページを同期
- 認証付きで候補ページの存在を確認
- ミニCosenseパネルの最初のリクエストへ認証ヘッダーを追加
- HTTP 401と403を認証エラーと権限エラーに分けて表示
- Cosenseへの辞書書き込みは次の段階として継続
- バックグラウンド専用IMで設定ウィンドウへ入力できない問題を修正
- 設定表示中だけアプリをアクセサリとして有効化
- プロジェクトURLと認証情報の入力欄をファーストレスポンダへ設定
- アクセサリ表示中の設定入力欄で`Command＋V`を直接処理
- 認証ウィンドウへ「クリップボードから貼り付け」を追加
- アドホック署名の更新後にキーチェーン認証を通過できない問題を確認
- キーチェーン保存を廃止し、権限`0600`のユーザー用認証ファイルへ変更
- 旧キーチェーン項目を読み込まず、パスワード要求を回避

### Cosenseパネルのログインと配置

- Cosenseパネルを操作可能なキーパネルへ変更
- IMをDockへ表示しないアクセサリアプリとして実行
- 永続的なWebKitデータストアでログインCookieを保持
- Web表示ではPATではなくCosenseのWebログインセッションを使用
- 辞書パネルの配置後フレームを基準にCosenseパネルを配置
- 左右と上下の大まかな配置を維持しながらパネルの重なりを防止
- 表示領域が狭い場合はCosenseパネルの高さを利用可能範囲へ縮小
- CosenseのクリックでIMが非アクティブ化され、パネルが閉じる問題を修正
- Cosenseクリック時のIM終了処理を抑止
- Cosenseの高さを辞書パネルに合わせて縮小する処理を廃止
- 候補パネルと辞書パネルを結合した領域の左または右へCosenseを配置
- WebKitを非アクティブ型パネルへ配置したことで動作が重くなる問題を修正
- Cosenseを通常の操作可能パネルへ戻し、クリック判定だけで表示を維持
- プロジェクト設定後に独立したCosenseログインウィンドウを表示
- 入力ソースメニューへ「Cosenseへログイン…」を追加
- ログインウィンドウと候補選択時のパネルでCookieを共有
- ログインウィンドウでプロジェクト編集画面ではなくログイン専用ページを表示
- ログインウィンドウを閉じた時にWebViewを破棄してCPUとメモリを解放
- Cosenseログインをmyim本体から補助アプリの別プロセスへ分離
- ログイン完了時にCosenseのCookieだけを権限`0600`の共有ファイルへ保存
- 候補ページを表示する直前に保存済みCookieをWebKitへ読み込み
- 補助アプリ終了後にGoogle認証を実行したWebKitプロセスをmyimから切り離す
- ページ存在確認がWebログインCookieを使用せず表示を中止する問題を修正
- Cookieをページ存在確認より先にWebKitへ読み込み
- 保存Cookieが変化した場合は同じ候補ページでも再読み込み
- 有効なCookieと古いPATの同時送信でページ存在確認に失敗する問題を修正
- Web Cookieが保存されている場合はページ存在確認でPATよりCookieを優先
- URLSessionの自動Cookieが保存済みCookieへ混在する問題を修正
- Cookie認証時は自動Cookie処理を無効化し、保存Cookieだけを送信
- Cosenseパネルの最初のページ要求にもCookieヘッダーを明示
- Cosenseクリック時にIMKの非アクティブ化処理が残っていた問題を修正
- WebViewがウィンドウを有効化する最初のクリックを直接受け取るよう変更
- `Command＋V`の辞書登録が物理キーコードだけに依存する問題を修正
- キー配列適用後の文字情報でも辞書登録ショートカットを認識
- `Command＋V`が入力先アプリへ先に渡り、登録語句が2回入力される問題を修正
- 辞書登録ショートカットをmyimで先に処理し、通常の貼り付けを抑止
- macOSの貼り付けメニューがIMの戻り値より先に実行されることを確認
- 辞書登録後の`insertText`を廃止し、標準の貼り付けだけで登録語句を確定
- 標準の貼り付け前に入力中のローマ字と候補パネルを消去
- Cosense APIが存在しないページにもHTTP `200`を返す問題へ対応
- `persistent`と`commitId`を検証し、未作成ページのCosenseパネルを非表示
- ひらがな1文字になる入力では、ひらがなと片仮名をローカルユーザー辞書より優先
- 2文字以上になる入力の候補順は変更せず維持
- ローカルユーザー辞書を通常入力の最優先階層として維持
- 同じ候補階層では直近に選択した候補を優先
- 候補選択履歴を権限`0600`のJSONへ保存し、再起動後も利用
- 語頭の濁音と半濁音を清音へ戻した辞書検索を追加
- `doori`から`toori`も検索し、`どおり`と`通り`を候補へ追加
- 候補未選択の入力中はローマ字をページ名としてCosenseを検索
- 候補選択後は選択候補をページ名とする検索へ切り替え
- 候補選択解除後は入力中のローマ字ページへ戻す
- ローマ字ページの表示中はOS辞書パネルを表示しない

## 2026-08-02

### 実装予定の追加

- 候補未選択の入力中に`Tab`で辞書登録モードへ移行する案を追加
- 1回目の`Return`で確認、2回目の`Return`で保存と確定を行う操作を追加予定
- 辞書登録モードへ操作段階と次のキーを示す案内表示を追加予定
- 選択中のユーザー辞書候補を`Command＋X`で削除する操作を追加予定
- 削除候補をクリップボードへコピーし、誤削除時に再登録できる保険を追加予定
- 入力と確定の履歴から次の入力を推薦する機能を追加予定
- 履歴件数の上限、古い履歴の間引き、集計後の原文削除による肥大化対策を追加予定
- Cosense専用パネルを汎用の外部情報パネルへ分離予定
- Cosense、Wikipedia、その他の情報ソースを選択可能にする予定
- 入力先アプリで選択した文字列をユーザー辞書へ登録する機能を追加予定
- Google検索サジェスチョンを変換候補へ追加し、選択候補から検索を開く機能を追加予定
- Google検索候補は入力内容の外部送信を明示し、初期状態を無効にする予定
- コア機能と実験機能を分離し、実験機能を個別に有効化できる設定を追加予定
- 実験機能の障害がローカル変換を停止させない構成へ分離予定

### 次入力候補

- 確定語句の前後関係をローカルで集計する学習モデルを実装
- 確定直後に学習済みの次入力候補を最大7件表示
- 矢印キーで選択し、`Return`で確定する操作を追加
- 通常入力、Space、`Escape`で次入力候補を閉じる処理を追加
- 入力ソースメニューへ「次入力候補を使用」を追加
- 入力ソースメニューへ「次入力履歴を削除」を追加
- 生の時系列ログを保存せず、前後関係の回数と最終使用順だけを保存
- 文脈数を最大256、各文脈の候補を最大8件、語句を80文字までに制限
- 上限超過時に古い文脈と低頻度候補を自動削除
- IMの切り替え、再起動、機能オフでは入力系列を分断
- 次入力候補の選択中もCosenseとOS辞書のプレビューを更新
- 集計データを権限`0600`の`next-input-model.json`へ保存
- `Tab`で次入力候補パネルへ移り、先頭候補を選択する操作を追加
- 候補選択後は`Tab`とSpaceで次候補へ移動
- `Shift＋Tab`で前候補へ移動
- 矢印キーで候補配置に沿って上下左右へ移動
- 候補選択前のSpaceは従来どおり半角Spaceとして入力

### 選択文字列の辞書登録

- 入力ソースメニューへ「選択文字列を辞書登録…」を追加
- IMKTextInputから入力先アプリの選択範囲と文字列を取得
- 選択文字列を取得できない場合はクリップボードへ自動切り替え
- ローマ字の読みを入力する登録ダイアログを追加
- 登録後は選択文字列を置換せず、ユーザー辞書だけを更新
- 複数行選択の改行と連続空白を単一Spaceへ正規化
- 読みに使用できる文字をASCII英字、ハイフン、アポストロフィへ制限
- 既存の`Command＋V`登録と選択文字列登録で辞書更新処理を共通化
- 同じ正規化読みへ候補を追加し、重複候補は追加しない処理をテスト
- 次入力候補のキャンセル後もCosenseまたはOS辞書パネルが残る問題を修正
- 次入力候補の解除時に候補、Cosense、OS辞書の全パネルを閉じる処理へ統一
- 次入力候補がASCII文字入力以外では自動終了しない問題を修正
- キー処理の先頭で文字、記号、ショートカット入力による解除を判定
- InputMethodKitの`commitComposition`でも次入力候補と関連パネルを終了
- Tab、Space、矢印、Return、Escapeは次入力候補の操作として維持
- 候補未選択のローマ字入力中に`Tab`で辞書登録モードへ移る機能を実装
- 登録候補の直接入力と`Command＋V`による貼り付けに対応
- 1回目の`Return`で確認し、2回目で保存と本文への確定を行うフローを追加
- 辞書登録の操作段階と次のキーを候補パネルへ表示
- `Escape`で登録を破棄し、元の入力と通常候補へ戻る処理を追加
- Tab辞書登録を`Return`1回で保存と本文への確定を行う操作へ変更
- 登録案内を短い3項目へ分割し、候補パネル内で文章が切れにくい表示へ変更
- Tab辞書登録モードの候補入力で通常のローマ字変換が働かない問題を修正
- 登録前の読みを保持し、候補入力側で通常候補の検索、Space、矢印選択を利用する構造へ変更
- Tab辞書登録中の1回目の`Return`では候補を決定し、登録モードを維持するよう変更
- 2回目の`Return`でユーザー辞書への保存と本文への確定を行うよう変更
- 選択中の次入力候補を、新しい文字または記号の入力前に自動確定する処理を追加
- Tab辞書登録で変換部分を`Return`により決定した後、続きの文字を入力できない問題を修正
- 決定済みの文字列を保持しながら次の変換部分を入力し、空の状態で`Return`を押すと登録を完了する方式へ変更
- ASCII外の記号入力が候補の自動確定より先に通常入力へ返される問題を修正
- 通常変換とTab辞書登録の候補選択中に記号を入力した場合、選択候補を確定して記号を次の入力として扱う処理を追加
- 次入力候補パネルを無操作のまま5秒経過すると自動終了するタイマーを追加
- 次入力候補を操作した場合は自動終了までの待機時間を数え直す処理を追加
- 選択中のユーザー辞書候補を`Command＋X`で削除する機能を実装
- 削除した候補をクリップボードへコピーし、`Command＋V`で再登録できる保険を追加
- 同じ候補が基本辞書やCosense拡張辞書に存在しても、ローカルユーザー辞書の登録だけを削除する処理を追加
- 最後の候補を削除した読みはユーザー辞書から削除する処理とテストを追加
- 次入力候補の未選択時にSpaceを入力しても候補パネルを維持するよう変更
- 次入力候補の未選択時は矢印キーを入力先アプリへ渡し、`Tab`で選択した後だけ候補移動に使うよう変更
- Tab辞書登録で変換部分が空のときにSpaceを押すと、登録文字列へ空白を追加する処理を追加
- `kakujuu`から`拡充`が出ないような基本辞書の不足を追加辞書レイヤーで補完する実装予定を追加
- 欠落候補の診断コマンド、公開辞書の変換、回帰テスト蓄積の方針を追加
- 半角、全角、日本語組版向け記号を同じ入力から選べる記号候補拡充の予定を追加
- `Control＋T`による現在時刻入力と連続入力による書式切り替えの予定を追加
- `YYYYMMDD`などの指定書式を現在日時へ置換する機能の予定を追加
- ローカル変換、基本辞書、ユーザー辞書、拡張辞書キャッシュを常時有効なコア機能として整理
- 次入力候補、Cosenseパネル、macOS辞書パネルを個別に切り替えられる「実験機能」サブメニューを追加
- Cosenseパネル無効時はページ存在確認とWebView更新を実行しない処理を追加
- macOS辞書パネル無効時は語義抽出を実行しない処理を追加
- 実験機能の設定を保存し、再起動せず現在の入力へ反映する処理を追加
- コア機能をローマ字入力と候補生成だけに縮小
- TKGJE基本辞書、ローカルユーザー辞書、Cosense拡張辞書、英語補完を個別に切り替えられる実験機能へ変更
- すべての辞書候補を無効にしても、ひらがな、片仮名、記号候補を生成する構成を維持
- InputMethodKitのサブメニュー内では実験機能の操作がコントローラーへ届かず、設定値が保存されない問題を修正
- 実験機能をトップレベルのチェック項目へ変更し、各target-actionが直接実行される構成へ変更
- Google検索候補を初期状態が無効の実験機能として実装
- 2文字以上の入力を250ミリ秒待って非同期取得し、入力変更時に古いリクエストを破棄する処理を追加
- Google候補の通常確定と`Command＋Return`によるブラウザ検索を追加
- Google候補の通信失敗や応答形式変更がローカル変換を停止させない構成を追加
- 候補取得URLの生成、JSON解析、重複除去のテストを追加
- Google候補応答が既定ではShift_JISになることを実応答で確認し、入出力文字コードをUTF-8へ固定
- Google検索候補の取得とブラウザでのGoogle検索を別の実験機能へ分離
- Google候補取得を無効にした状態でも、ローカル候補を`Command＋Return`で検索できるよう変更
- Google検索URL生成をコア処理へ分離し、候補取得から独立して動作するテストを追加

### 公式外部候補とWeb検索

- 非公開Google検索候補エンドポイントの実装を削除
- Wikipedia OpenSearch APIによる日本語記事名候補を追加
- macOS 15以降のTranslation frameworkによる日本語から英語への候補を追加
- Azure Translator Dictionary Lookupによる複数英訳候補を追加
- 外部候補を個別にオン・オフできる実験機能を追加
- Azure APIキーを権限`0600`のローカルファイルへ保存
- 入力変更から250ミリ秒後に問い合わせ、古い処理を破棄する構成を維持
- 外部サービスの失敗時もローカル候補を継続
- Web検索URLを`%s`プレースホルダーで設定する機能を追加
- 任意の候補を`Command＋Return`で確定して設定先を開く機能を追加
- 検索候補取得とブラウザ検索のオン・オフを独立して維持
- Apple翻訳のメニュー名を「日本語入力から英語の変換候補を取得」へ変更
- Web検索のメニュー名を実際のキー操作に合わせて「Command＋ReturnでWeb検索」へ変更
- 今後のメニュー名を具体的な操作と結果に基づいて命名する方針を追加

### 漢字候補の補完と動詞活用

- TKGJEとは独立したmyim補完辞書レイヤーを追加
- `kakujuu`から`拡充`を候補表示
- 補完辞書をTKGJE更新後も維持する構成を追加
- 補完候補を既存辞書より後、かな候補より前へ配置
- 一段動詞のて形、た形、否定形、過去否定形、丁寧形、希望形を追加
- 五段動詞の否定形と丁寧形を追加
- `する`と`来る`の不規則活用を追加
- `kangaete`から`考えて`を生成する回帰テストを追加
- 品詞と活用型を持つ辞書データへ移行する今後の方針を文書化

### 複数OSS辞書による基本辞書

- Mozc OSS辞書をかな漢字変換の主辞書として追加
- TKGJEを学習者向けの日英補助辞書として継続
- Mozcの約129万行を候補コストで圧縮する変換スクリプトを追加
- 約28万読み、約38万候補、9.9MBのかな漢字辞書を生成
- 読みごとの候補をMozcコスト順で最大32件へ制限
- かな読みの二分探索エンジンを追加
- 前方一致候補を最大2048件に制限し、入力中の負荷を抑制
- MozcとTKGJEの終止形を動詞活用生成へ利用
- Mozc辞書固有の通知とライセンスをアプリへ同梱
- 辞書の出典と配布条件を`docs/DICTIONARY_LICENSES.md`へ追加
- 基本辞書メニューを「ローカル基本辞書で変換候補を取得」へ変更
- Mozc OSSとTKGJEの原データから必要項目だけを抽出した派生辞書であることをREADMEへ明記
- TKGJEでスラッシュ併記された別表記を独立した変換候補へ分割

### READMEの再構成

- 現在の入力動作、辞書構成、ユーザー機能、外部連携、導入手順の順へ整理
- Mozc、TKGJE、myim補完辞書、ユーザー辞書、Cosense拡張辞書の用途を表で比較
- MozcとTKGJEから抽出する項目と保持しない項目を明記
- TKGJEは英語の語義や用例を抽出せず、英訳候補に使用しないことを明記
- 重複していた外部辞書、漢字補完、動詞活用の説明を辞書構成へ統合
- 次入力履歴とmacOS辞書パネルの説明を適切なセクションへ移動
- 実装予定を現在の動作から独立したREADMEセクションとして維持
- 外部辞書データ比較とライセンス資料を現在の採用構成へ更新

### プレースホルダー候補の表示

- `〜など`のような候補では確定時だけでなく候補パネル上でも`〜`を非表示
- 候補の内部値とCosenseや辞書の検索対象は変更せず、表示と確定だけを正規化
- 単独の`〜`は記号候補として維持

### 基本辞書のプレースホルダー正規化

- MozcとTKGJEから抽出する段階で複合候補内の`〜`と`～`を除去
- `〜など`は基本辞書へ`など`として保存
- 正規化後に同じ表記になる候補を読みごとに重複除去
- 同梱済みのMozc基本辞書とTKGJE基本辞書も同じ形式へ更新
- 既存のユーザー辞書、Cosense辞書、更新済みTKGJEキャッシュも読み込み時に正規化

### UndoとRedoの透過処理

- 未確定入力中の`Command＋Z`でローマ字を確定してからUndoしていた処理を廃止
- `Command＋Z`と`Shift＋Command＋Z`では未確定状態と補助パネルだけを閉じる
- キーイベントを消費せず入力先アプリへ渡し、通常のUndoとRedoを実行

### 全角記号の拡充

- ASCII記号の候補へ入力した半角表記そのものを追加
- 全角記号、句読点、括弧、引用符、長音、ダッシュ、数学記号、組版記号を同じ入力から選択可能に変更
- `#`、`$`、`%`、`&`、`*`、`+`、`=`、`@`、`^`、バッククォート、縦線の変換候補を追加
- 半角、全角、日本語記号の候補順へ既存の選択履歴を適用
- `zh`、`zj`、`zk`、`zl`など既存のz記号入力を維持

### Apple Translation候補の文書化

- ローマ字からひらがなへ変換して日本語から英語へ翻訳する処理をREADMEへ明記
- 250ミリ秒の待機、古い処理の破棄、翻訳結果1件の追加手順を記載
- SwiftUIの`translationTask`を透明な補助パネル上で実行する構成を記載
- macOS 15以降、言語モデル、失敗時のローカル候補継続、APIキー不要の条件を記載


## 2026-08-03

### 外部情報パネルの汎用化

- Cosense専用パネルを共通WebViewの外部情報パネルへ変更
- `%s`を検索語に置換するURLテンプレート設定を追加
- 候補選択前は入力中のローマ字、候補選択後は選択候補で検索
- Wikipedia、Google、CosenseなどのWeb検索とサイト内検索に対応
- WebViewのログインCookieを保持し、`scrapbox.io`では保存済みCosense Cookieも使用
- 外部情報パネルのサイト固有APIとページ存在確認を廃止
- macOS辞書パネルを外部情報パネルから独立して表示
- 外部情報パネルの表示待機時間を即時、0.5秒、1秒、2秒、3秒、5秒から選択可能に変更
- 入力や候補が変わるたびに表示待機をリセットし、古い検索ページの遅延表示を防止
- 検索ページは入力時に即座にバックグラウンドで読み込み、パネル表示だけを設定時間まで待機

### 入力ソースの名前とアイコン

- 日本語入力モードキーとmyim固有の入力ソースIDを分離
- メニューバーとカーソル位置では透過テンプレートの小文字`m`を継続使用
- システム設定用に青い背景と白い小文字`m`の独立したアプリアイコンを追加
- Bundle Versionと入力モードアイコン名を更新し、macOSの古い登録情報との混同を防止
- myim固有モードキーでは一覧追加後に日本語IMとして利用できない問題を確認
- macOSが日本語入力モードとして解釈する`com.apple.inputmethod.Japanese`キーへ復元
- 実際の入力ソースIDは`io.github.sendarionn.inputmethod.myime.Japanese`として分離
- カーソル位置インジケータが参照する入力モードTIFFを他の正常なIMと比較
- 32×32、144 dpiだけだった画像を、16×16、72 dpiと32×32、144 dpiを持つマルチ解像度TIFFへ変更
- テンプレート方式で正常表示されるmacSKKに合わせ、キャンバスを22×16と44×32の横長サイズへ変更
- 各解像度へ小文字`m`を直接描画し、拡大縮小による変形を防止
- SwiftyGyaimの単一日本語モード構成とアイコン参照を調査
- モード別TIFF、テンプレート、代替アイコン、パレットアイコン指定を廃止
- SwiftyGyaimと同じ`tsInputMethodIconFileKey`だけを使う構成へ変更
- 入力モードアイコンを青い背景と白い小文字`m`の22×22 PDFへ変更
- Bundle Versionを95へ更新し、古い入力ソースアイコンのキャッシュを回避
- 入力ソース追加画面で名前が空になる問題についてInfoPlist.stringsを比較
- myimだけがASCII形式で、macSKKとSwiftyGyaimはUTF-16またはplist形式である差を確認
- InfoPlist.stringsをビルド時にバイナリplistへ変換して格納
- Bundle IDを`io.github.sendarionn.inputmethod.myim`へ変更し、旧myime IDのTISキャッシュから分離
- 入力モードIDとInputMethodKit接続名も新しいmyim IDへ統一
- Bundle Versionを96へ更新
- 同じインストールパスに旧myime Bundle IDと新myim入力モードIDが混在する状態を確認
- インストール前に旧Launch Services登録を解除し、配置後に新バンドルを再登録
- TextInputMenuAgent、TextInputSwitcher、CursorUIViewService、imklaunchagentをインストール後に再起動
- 新しいmyim Bundle IDではシステム設定の追加候補に表示されない回帰を確認
- 表示実績がある`io.github.sendarionn.inputmethod.myime`へBundle IDと接続名を復元
- バイナリ表示名、単一PDFアイコン、キャッシュ再登録処理は維持
- Bundle Versionを97へ更新
- メニューバーへ追加できても入力できない状態のInputMethodKitログを調査
- `IMKLaunchAgent -getIMKXPCEndpointForBundle: NO Endpoint`を確認
- アイコンキャッシュ対策で追加したimklaunchagentの強制終了がIM接続を破壊していたと特定
- TextInputMenuAgent、TextInputSwitcher、CursorUIViewService、imklaunchagentの強制終了を撤去
- 動作時に存在したTISUnifiedUIForInputMethodEnabling、DefaultState、IsVisibleを復元
- アイコン構成はSwiftyGyaim方式の単一PDFとして分離
- Bundle Versionを98へ更新
- 接続再構築後、TIS選択だけでは入力先アプリが要求するまでmyimが起動しない正常な遅延起動を確認
- インストーラーによるmyim.appの手動起動とLaunch Services登録解除を撤去
- バンドル配置とTISRegisterInputSourceだけを行う最小構成へ復元
- `IMKServer`の保持処理がイベントループ終了後に置かれていた問題を修正
- イベントループ全体を`withExtendedLifetime`で囲み、InputMethodKit接続を実行中保持
- `IMKServer`初期化の成功と失敗をシステムログへ記録
- Bundle Versionを99へ更新
- 入力ソース名と入力不能の回帰をアイコン対応時の入力ソース定義変更まで追跡
- `InfoPlist.strings`の独自バイナリ変換を廃止し、動作実績のある形式へ復元
- モード別TIFF参照を含む動作実績のある入力ソース定義へ復元
- 入力コントローラー生成とキーイベント受信の診断ログを追加
- Bundle Versionを100へ更新
- 再登録後にmyimの日本語入力モードを明示的に選択
- 全IMの切り替えチップが非表示になる状態を調査
- 7日前から残っていた`CursorUIViewService`が`failed to decode send port`を繰り返していることを確認
- 応答しない該当プロセスを一度だけ再生成し、XPC listener接続の復旧を確認
- システムUIプロセスの終了処理はインストーラーへ追加しない方針を維持
- 入力切り替えチップが全体アイコンの44×32 TIFFを処理できず非表示になる問題を修正
- メニューバー用モードアイコンはTIFFのまま維持し、チップ用全体アイコンを16×16 PDFへ分離
- Bundle Versionを101へ更新
- メニューバー用TIFFと切り替えチップ用PDFを透明背景の単色テンプレートへ統一
- `TISIconIsTemplate`によってライトモードとダークモードの着色をmacOSへ委任
- アプリアイコンは青い背景と白い小文字`m`の配色を維持
- テンプレート素材を新しいファイル名へ変更して過去のアイコンキャッシュと分離
- READMEのアイコン形式と`InfoPlist.strings`の説明を現在の実装へ更新
- Bundle Versionを102へ更新
- テンプレート素材名をmacOSの規則に合う大文字`Template`接尾辞へ変更
- 入力モードアイコンを角丸四角形から小文字`m`を透明に切り抜くデザインへ変更
- Bundle Versionを103へ更新
- RGBのテンプレートTIFFが通常画像として表示されていた問題を調査
- macSKKと同じアルファ付きグレースケールTIFFへ変更
- メニューバー用22×16と切り替えチップ用16×16の素材を分離
- Apple純正IMと同じ命名の`@2x.pdf`を高解像度表示用に追加
- Bundle Versionを104へ更新
- `ditto`の上書きで旧アイコン素材が残り続ける問題を修正
- インストール時にユーザー領域の既存`myim.app`だけを置換してからコピー
- `.build/myim.app`にも過去のResourcesが蓄積していた問題を修正
- ビルド開始時に生成対象のIMバンドルとアイコン作業ディレクトリだけを再作成
- インストール済みTIFFを白背景へ合成し、角丸四角形と透明な`m`が画像データに存在することを確認
- 表示が変わらない原因をText Input UIプロセスのメモリ内アイコンキャッシュと特定
- `TextInputMenuAgent`、`TextInputSwitcher`、`CursorUIViewService`を一度だけ同時に再生成
- 再生成後のチップ用XPC listener接続とmyim再選択の成功を確認
- メニューバーアイコンの縦長変形と位置ずれを調査
- メニューバー用44×32 PDFを廃止し、macSKKと同じ22×16 TIFFだけを使用
- チップ用高解像度PDFをApple純正IMと同じ28×36キャンバスへ変更
- チップの角丸四角形を中央配置し、上下余白を確保
- Bundle Versionを105へ更新
- メニューバーアイコンがRetina環境で拡大されて粗くなる問題を修正
- 22×16と44×32を1倍、2倍の順で格納したGray TIFFへ変更
- カーソルチップがテンプレート着色を適用せず黒画像を表示する問題へ対応
- チップ用代替アイコンとして白い28×36 PDFを追加
- Bundle Versionを106へ更新
- カーソルチップが代替アイコンを参照しないことを実表示から確認
- チップ用の通常アイコンと代替アイコンを同じ白い28×36 PDFへ変更
- メニューバー用テンプレートTIFFは変更せず分離を維持
- Bundle Versionを107へ更新
- 白いチップPDFがバンドル全体の`TISIconIsTemplate`によって黒へ再着色される問題を特定
- バンドル全体のテンプレート指定を削除
- メニューバーだけを`Template`接尾辞で自動着色し、チップは白い通常画像として分離
- Bundle Versionを108へ更新
- CursorUIViewServiceが`KLInputSourceIconManager`の`indicatorImageForInputSource:`を使うことを特定
- myim、macSKK、SwiftyGyaim、Apple日本語IMを同じ内部APIで比較
- myimとSwiftyGyaimは`template=false`、macSKKとApple日本語IMは`template=true`である差を確認
- チップは親の`tsInputMethodIconFileKey`ではなく子入力モードのメニュー画像を使うことを確認
- `TISIconIsTemplate`を復元し、子入力モード画像を白へ着色させる構成へ修正
- Bundle Versionを109へ更新
- plist復元後も内部APIがmyimを`template=false`として返すことを確認
- Apple予約モードキーを流用するmyimとSwiftyGyaimがともに`template=false`になる構造差を特定
- 入力モード辞書キーと入力ソースIDを独自の`io.github.sendarionn.inputmethod.myime.RomanKanji`へ変更
- 古い入力モードIDのTISキャッシュから分離
- Bundle Versionを110へ更新
- 独自入力モードIDでも内部APIが`template=false`のままであることを確認
- `TISIconIsTemplate`と代替アイコンキーを同時指定していた競合を特定
- モードと入力メソッドの代替アイコン指定を削除し、テンプレート方式へ一本化
- 未参照になった白いメニュー選択画像の生成を削除
- Bundle Versionを111へ更新
- 独自入力モードIDが表示プロセス再生成後にTISから消える回帰を確認
- 動作実績のある`com.apple.inputmethod.Japanese`モードキーと旧入力ソースIDへ復元
- 代替アイコンキーの削除だけを維持
- Bundle Versionを112へ更新

### 入力時のWebView負荷軽減

- 入力ごとにWebViewを即時読み込みしていた処理を調査
- myim本体約243 MB、関連WebContent約183 MBの常駐メモリを確認
- 最新の検索URLは即時記録し、WebView読み込みを200ミリ秒まとめる処理へ変更
- 次の入力時に進行中のWebView読み込みを明示的に停止
- Cosense Cookieを5秒間メモリへ保持し、入力ごとの同期ファイル読み込みを削減

### 候補生成の負荷軽減

- 選択履歴がない候補を含む全件比較ソートを廃止
- 履歴がある候補だけを直近選択順へ並べ替え、未選択候補の辞書順を維持
- 完全一致候補の除外を配列検索からSet検索へ変更
- 最終候補の重複除去で中間配列の生成を削減
- 全候補を内部保持し、8件目以降へ移動できる仕様を維持

### Mozc辞書のメモリマップ検索

- 約94万行のMozc辞書を起動時に全オブジェクト化する処理を廃止
- 辞書ファイルをメモリマップし、約28万件の読み行オフセットだけを索引化
- UTF-8バイト列の二分探索で完全一致と前方一致を検索
- 検索対象の候補だけをStringへ変換し、候補順と件数制限を維持
- 境界位置、完全一致、前方一致、件数制限、読み数の回帰テストを追加

## 2026-08-09

### 曖昧検索の第1段階

- ローマ字読みの挿入、削除、置換、隣接文字入れ替えに対応する検索エンジンを追加
- 既存のローマ字表記正規化を曖昧検索にも適用
- 入力長に応じて許容距離を制限し、短い入力での過剰な候補生成を防止
- 距離、共通接頭辞、辞書順で曖昧候補を順位付け
- 通常候補へ混在させず、独立した単体テストで検証する構成を追加
- 実験機能として個別にオン・オフできる「もしかして？」パネルを追加
- 直接一致する辞書候補がある場合は曖昧候補を表示しない抑制処理を追加
- 曖昧候補を通常候補へ混在させず最大3件まで別表示
- Option＋下矢印でのみ「もしかして？」候補の選択へ移る操作を追加
- 矢印キーへ自動付与されるfunction修飾を除外し、Option＋下矢印を正しく判定
- Spaceと矢印による移動、Return確定、Escapeによる通常候補への復帰を追加
- 曖昧候補の選択中に次の文字が入力された場合は選択候補を確定
- 曖昧候補へ移る操作にShift＋Tabを追加し、Option＋下矢印が競合するアプリでも選択可能に変更
- Option＋下矢印の判定対象を主要な修飾キーに限定し、環境依存のフラグによる判定漏れを防止
- 曖昧候補へ移るOption＋下矢印を廃止し、Shift＋Tabへ一本化
- 曖昧候補の選択中にTabを押すと通常候補へ戻る操作を追加
- 曖昧候補から通常候補へ戻る操作をEscapeだけに変更
- 通常候補の選択と順送りをSpaceからTabへ変更
- Spaceは入力内容を確定して半角空白を入力する動作へ変更
- 辞書登録モードの開始操作をTabからOption＋Returnへ変更
- 辞書登録完了時に未確定文字列と確定文字列が二重入力される問題を修正
- 登録完了時は現在の未確定範囲を登録語句で明示的に置換して1回だけ確定
- 辞書登録の最初のReturnで候補が確定済み文字列として残る原因を修正
- 未確定文字列の更新時も現在の未確定範囲を明示的に置換し、古い表示の確定を防止
- 辞書登録途中のReturnでは入力先へ未確定文字列を再送信しないよう変更
- 入力部分が空の状態で押した最後のReturnだけで辞書保存と本文確定を行う状態遷移へ修正
- 辞書登録モード中の候補パネルへ「登録する語句を入力中…」を表示
- 途中のReturnによる入力追加と最後のReturnによる辞書登録確定を別の案内として表示
- 曖昧検索へ母音前と末尾の二重nを単一nへ寄せる補助読みを追加
- `genninn`から辞書読み`genin`を検索し「原因」をもしかして候補へ表示
- 元の読みも同時に検索し、正規の二重nを含む読みを維持
- 曖昧検索の補助読みに`n'`区切り、唇音前のm/n、長音表記差を追加
- 辞書側と入力側の双方を補助読みで索引化し、複数の入力揺れが重なる検索に対応
- `gen'in → 原因`、`shimbun → 新聞`、`tokyo → 東京`の回帰テストを追加
- 長音記号を含む入力で、片仮名の直接変換と基本辞書の完全一致を活用候補より優先
- `ki-`で`聞い`より`キー`を先に表示する候補順へ調整
- ユーザー辞書とCosense拡張辞書の既存優先順位を維持
- READMEへインスピレーション元としてGyaimとSKKを明記
- READMEの実装予定を現在のコードと既知の実機課題に合わせて再整理
- 安定化、曖昧検索と意味検索、ユーザー辞書同期、外部情報パネル、日時入力、Windows対応へ分類
- 辞書登録の実アプリ検証、カーソル位置アイコン、曖昧検索の性能計測を優先修正項目として明記
- Cosenseへのユーザー辞書書き戻しを実装予定から削除
- 曖昧検索を既存機能の強化、意味検索を新しい実験機能として区別して記載
- 意味検索専用の`SemanticDictionaryEntry`とJSONL読込・書出しを追加
- TKGJE個別項目から見出し、読み、品詞、語義、説明、出典だけを抽出する変換スクリプトを追加
- 用例全文と管理情報を意味辞書へ取り込まない軽量な派生形式を採用
- 見出し、読み、語義を検索するローカル意味辞書エンジンと回帰テストを追加
- ベクトル生成と「もしかして？」パネルへの接続を次段階としてREADMEへ明記
- TKGJE現行リビジョンから30,296項目の意味検索用JSONLを生成して同梱
- 語彙階層を意味辞書へ保持し、Basic・Coreだけを初期ベクトル索引の対象に限定
- Natural Language frameworkの英語文埋め込みで語義と説明をバックグラウンド索引化
- 日本語入力をApple Translationで英訳し、英語語義との類似度をローカル検索
- 意味候補を最大3件の独立した「意味でもしかして？」パネルへ接続
- 意味検索を初期無効の実験設定として追加し、入力変更時の翻訳と検索をキャンセル
- 実機で日本語文埋め込みが利用できず英語文埋め込みだけが利用可能なことを確認し、英訳経由の構成を採用
- 実機で英語文埋め込みも取得できず意味検索が開始されない根本原因を確認
- 利用可能な300次元の英語単語埋め込みへ変更
- Basic・Core・Generalを含む30,295項目のFloat16ベクトル索引を事前生成して同梱
- 全項目の重心を除去して単語ベクトル平均の共通方向による誤候補を抑制
- 保存済み索引をメモリマップで読み込み、起動時の30,295項目のベクトル再計算を廃止
- 連続したローマ字入力を辞書読みに分割し、語義を検索文へ補う処理を追加
- `tatemonowosekkeisuruhito`から`tatemono`、`sekkei`、`hito`を抽出し、翻訳結果だけでは失われる建物・設計・人の意味を補完
- 意味候補の独立した見出しを廃止し、通常の「もしかして？」パネルへ統合
- 表記揺れ候補を先、重複しない意味候補を後に並べて同じ操作で選択可能に変更
- READMEを主要機能、基本操作、設定、インストール中心の短い構成へ再編
- 辞書生成、変換調整、macOS辞書の詳細説明を既存の個別資料への参照へ整理
- 重複していた実装内部、アイコン仕様、外部機能の手順説明をREADMEから削除
- 辞書資料の表題を検討資料から現在の辞書構成へ変更し、通常変換辞書と意味辞書の説明を区別
- MISCELLANEOUSの候補生成順を現在のMozc、TKGJE、補完辞書レイヤーに合わせて更新
- 入力中のOption＋Deleteで直前の単語単位を削除する操作を追加
- 入力中のCommand＋Deleteで未確定文字列全体を削除する操作を追加
- 辞書登録モードの入力中と確定済み登録文字列にも修飾Deleteを適用
- 削除後に候補と外部表示を更新する処理とコア単体テストを追加
- ひらがなと片仮名を入力の長さにかかわらず候補の最優先へ変更
- 完全一致する直接候補を選択履歴より前へ固定
- 直接候補の後を選択履歴の直近順、未選択のその他候補の順へ再構成
- ユーザー辞書、Cosense、Mozc、TKGJE、活用、補完辞書の完全一致を共通の直接候補として分類
- 候補グループ間の重複除去と順位を検証するコア単体テストを追加
- ひらがなと片仮名の最優先を、ひらがな1文字分になる入力だけへ限定
- 2文字分以上では完全一致候補、直近候補、かな候補、その他候補の順へ調整
- 入力の長さによる候補グループ切替の回帰テストを追加
- F6でひらがな、F7で全角カタカナ、F8で半角カタカナへ直接変換する操作を追加
- F9で全角英数字、F10で半角英数字へ直接変換する操作を追加
- ファンクションキー変換後も未確定状態を維持し、Returnで確定、Escapeでローマ字へ復帰する構成を採用
- 一段動詞と五段動詞の可能形、受身形、使役形の逆引き規則を追加
- `mirareru`から`見られる`、`kakeru`から`書ける`を生成する回帰テストを追加
- F6〜F10で通常の候補選択処理を呼び、補助パネルまで更新していた処理を廃止
- ファンクションキー変換では入力先の未確定範囲だけを書き換え、候補と外部情報パネルを閉じてフォーカスを維持する方式へ変更

### 入力ソースメニューの整理

- InputMethodKitがサブメニュー内のコマンドを入力セッションへ配送しない挙動を実機ログで確認
- 動作しない階層メニューを廃止し、独立した「myim設定」ウィンドウへ設定項目を移動
- 入力ソースメニューを設定、辞書登録、Cosense更新、状態確認へ限定
- 設定ウィンドウを「変換候補」「外部表示」「辞書管理」の順に整理
- 各機能のオン・オフ設定とキーボードショートカットを維持

### 外部情報パネルから候補選択への復帰

- WebViewのクリックとスクロールから操作開始を明示的に検出
- WebViewへイベントが届く前のキーウィンドウ切替時点で操作状態を保持
- マウス位置による操作状態の推測を廃止
- 外部情報パネルの操作中は未確定文字列、候補一覧、選択位置を保持
- 操作中に届いた表示更新を保留し、WebViewの再読込による操作中断を防止
- キーウィンドウ化がInputMethodKitの入力セッション切替を起こすことを特定
- 外部パネルがキーウィンドウを奪わず、クリックとスクロールだけを受け取る構成へ変更
- Escapeまたは次のキー入力でパネル操作状態だけを終了し、候補選択を維持

## 2026-08-10

### 日付と時刻の動的候補

- Control＋Tで時刻を直接入力する実装予定を廃止
- `kyou` `kinou` `ashita`などから対象日を動的に生成する候補を追加
- `ima` `jikoku` `genzaijikoku`から現在時刻を動的に生成する候補を追加
- 日付と時刻の複数書式を通常の候補選択で切り替える方式へ変更
- システムタイムゾーンを使った相対日付計算のコア単体テストを追加
- 日時候補を初期無効の実験機能として設定から切り替え可能に変更
- 日付、時刻、日時の候補書式を設定から個別指定する機能を追加
- macSKKの日付変換を参考に、読みと相対日から設定順で複数候補を生成
- `YYYYMMDD`などの日時トークンを安全に展開するコア処理を追加

### 選択履歴による候補順位

- 選択履歴ファイルが保存・更新されていることを実データで確認
- 完全一致候補が履歴順位の対象外だったため、同じ優先グループ内を直近選択順へ変更
- 完全一致候補をその他候補より優先する既存の大分類は維持

### リポジトリと変換処理の整理

- 追跡されていたPythonの`__pycache__`と`.pyc`を削除し再生成を除外
- 実運用から参照されていない旧`SortedConversionEngine`と専用テストを削除
- 現行`ConversionEngine`の接頭辞検索を全件走査から二分探索へ変更
- 候補選択履歴を専用のコア型へ分離し、保存件数を直近4,096候補へ制限
- 候補履歴と次入力履歴で重複していた保存処理を共通化
- 全キー入力を記録していた診断ログを削除し、通常入力時のログI/Oを停止
- インストール時に必ず最新のリリースビルドを生成する単一コマンドへ統合
- Swiftテストに加えて辞書変換スクリプトのPythonテスト手順をREADMEへ追加
- README末尾のインスピレーション元を冒頭の紹介文へ移動
- 開発初期の検証用コマンドライン製品、専用ソース、テスト辞書を削除
- Package.swiftとREADMEからコマンドライン版の定義と操作手順を削除

### 入力中のmacOS辞書検索

- 候補選択前も入力中のローマ字をmacOS標準辞書で検索するよう変更
- 文字入力ごとに語義パネルをインクリメンタル更新
- 候補選択後は従来どおり選択候補へ検索対象を切り替え
- 同じ入力に対するDictionary Servicesの再検索を避ける128件のメモリキャッシュを追加

### 辞書パネルの配置

- 入力行を含めず候補パネルだけを基準にしていた重なりの原因を修正
- 入力行と候補パネルを結合した回避領域の上または下へ辞書パネルを配置
- 入力中と候補選択中の両方で同じ回避領域を使用

### 候補パネルの配置

- 入力行へ隙間なく接していた候補パネルに8ptの間隔を追加
- 間隔を含めて画面内に収まる上下位置を選ぶ配置計算へ変更

### ショートカット案内

- 候補パネル下部に現在利用できる操作の案内を追加
- 入力中、候補選択中、次入力候補、辞書登録で案内を切り替え
- もしかして候補の選択中にも移動、確定、復帰操作を表示
- 案内による横方向の拡大を抑え、短い表記を必要な高さだけ折り返す構成へ変更
- 案内で横幅が変わった後に候補の行数を再計算し、不要な縦方向の余白を削除

### 曖昧検索の撥音対応

- `gennin`を`genin`へ正規化して「原因」を表示する回帰テストを追加
- 別の完全一致候補がある場合も、正規化後の距離0候補は「もしかして？」へ表示
- 距離1以上の候補は完全一致候補がある場合に抑制する既存の優先制御を維持

### 外部情報ページをブラウザで開く操作

- 外部情報パネルのタイトルバーに「ブラウザで開く」を追加
- Command＋Oで現在表示中のHTTPまたはHTTPSページを既定ブラウザへ渡す操作を追加
- 候補パネルのショートカット案内へCommand＋Oを追加
- タイトルバーのボタンにもCommand＋Oのショートカット表記を追加

### 記号候補の関連度順

- 記号候補へ通常の候補選択履歴を適用していた処理を廃止
- 入力した記号、対応する全角記号、日本語組版向け記号の順を固定
- `(`では`（`を`「`、`『`より優先する回帰テストを追加
- 入力文字と同じ半角記号を候補から除外し、未選択時のReturn確定だけで入力可能な構成へ変更

### 数字変換候補

- ASCII数字の0〜20から丸数字、全角数字、漢数字を生成する処理を追加
- `1`では`①`、`１`、`一`、`壱`の順で候補を表示
- 入力した半角数字は候補へ重複表示せず、未選択時のReturn確定を維持

## 2026-08-11

### READMEの設計思想

- Cosenseのmyimページと関連するIM、Gictionary、接続辞書、macSKKページを再確認
- 日英のモード切替をなくし、ローマ字から必要な表記を選ぶ方針を明記
- 読みと出力の単純な組、個人辞書と共有辞書、入力から連続する辞書登録を設計原則として整理
- 学習、活用、曖昧検索、外部情報を単純なコアへ重ねる補助層として説明
- ローカル変換を基本とし、通信機能を任意にする現在の方針を追加

### もしかして候補の誤タイプ対応

- `ainai`から辞書読み`aimai`を検索して「曖昧」を表示する回帰テストを追加
- 別の完全一致候補がある場合も、同じ長さで編集距離1の候補を高信頼候補として表示
- 文字数が変わる距離1候補と距離2以上の候補を抑制する既存の優先制御を維持

### 補助パネルの配置と案内表示

- 「もしかして？」パネルの回避領域へ入力行と候補パネルの両方を含めるよう変更
- 画面端で表示方向が切り替わっても入力部分へ重ならない配置に修正
- 各パネルのショートカット案内を11ptのセカンダリ文字色へ統一
- 「もしかして？」の見出しと操作案内を分離し、見出しだけを太字で維持

### 短い入力の隣接キー補正

- 2〜3文字の入力でQWERTY配列の隣接キー誤入力を曖昧検索
- `eo`から`wo`を補助検索し「を」を「もしかして？」へ表示
- 4文字以上にもQWERTY配列の隣接キー誤入力検索を適用
- 補正読みは辞書索引へ直接照合し、読みごとの辞書走査を増やさない構成へ変更

### 選択履歴による候補順

- 選択履歴が候補グループ内だけに適用され、かな候補が直接一致候補を越えられない問題を修正
- 1文字分のかな最優先を維持し、それ以外では直接一致、かな、その他をまたいで履歴順を適用
- `deno`で「での」を選択した後は「出野」より「での」を優先

### 角括弧入力の候補順

- `[`の変換候補で`［`より`「`を優先
- `]`の変換候補で`］`より`」`を優先

### 数字とz記号入力の整理

- 数字候補へ入力した半角数字そのものを表示
- `z-`、`z[`、`z]`、`z,`、`z.`、`z/`の特殊記号変換を削除

### 外部情報パネルのログイン管理

- Cosenseログイン補助アプリとCookie共有ファイルを廃止
- ログイン画面と外部情報パネルで同じ永続WebKitデータストアを使用
- Cookieとセッション情報の管理をWebKitへ限定
- 旧形式のCookieファイルを起動時に削除
- ログイン画面の遷移をHTTPSとabout URLに限定
- Cosense専用のログイン設定とログインウィンドウを廃止
- 外部情報パネルをキー入力可能にし、ページ内の通常のログイン画面を使用
- 外部情報パネルの遷移をHTTPSとabout URLに限定
- キーウィンドウ化によってInputMethodKitの非アクティブ化処理と競合する問題を確認
- クラッシュを防ぐためIMプロセス内の外部情報パネルを非キーウィンドウへ復帰
- 外部情報WebViewを汎用ブラウザ補助プロセスへ分離
- HTTPS URLとパネル位置だけを権限`0600`の指示ファイルで連携
- Cookieや認証情報をIMプロセスと共有せずWebKitデータストア内に限定
- 外部情報パネル内のログイン欄へ文字入力できる構成へ変更
- 補助ブラウザのWebViewで最初のクリックを受け付け、クリック時だけパネルをキー入力先に変更
- 補助ブラウザから操作開始をIMへ通知し、操作中の非アクティブ化では入力状態とパネルを破棄しないよう変更
- プロセス間通知より非アクティブ化が先行する競合を確認
- クリック時刻を同期マーカーへ先に記録し、非アクティブ化時に直接確認する方式へ変更
- 再インストール時にmyim本体と補助ブラウザの終了完了を待ってからアプリを置換
- WKWebView内部の子ビューへクリックが渡り、WebView本体のイベント処理が呼ばれない場合に対応
- 補助ブラウザのパネル最上位でマウス、スクロール、キー入力を記録
- `deactivateServer`の後に`inputControllerWillClose`が無条件でパネルを閉じる経路を特定
- 入力コントローラ終了時にも外部情報パネル操作中の保持判定を適用
- IM側の終了経路だけでパネルを維持する方式を廃止
- 補助ブラウザがキーウィンドウの間はIMからの非表示命令を無視
- パネル自身が操作中の表示状態を管理する構成へ変更

### ユーザー辞書の典拠URL

- Webページの選択文字列を登録するとき、取得できたページURLを候補と読みの組へ関連付けて保存
- メニューバーの典拠ページ項目を削除
- 典拠付きのユーザー辞書候補を選択中だけ、候補パネルに「⇧⌘O 辞書登録元ページ」を表示
- `Command＋Shift＋O`で保存済みの典拠ページを既定ブラウザへ表示
- 典拠ページを外部情報パネルの自動検索先には使用しない構成へ変更
- 典拠付き候補の候補パネルへメニュー操作の案内を追加
- URLを公開しないアプリでは典拠なしの通常登録へ切り替え
- HTTPとHTTPSだけを受け付け、URL内の認証情報とフラグメントを保存前に除去
- 辞書候補の削除時に対応する典拠も削除
- 典拠メタデータを権限`0600`の`dictionary-sources.json`へ分離
- 保存、正規化した読みでの参照、削除、URL検証の回帰テストを追加
- ブラウザ本文を選択した場合は、`IMKTextInput`に加えてmacOSアクセシビリティの選択テキストを取得
- アクセシビリティ未許可で選択文字列を取得できない場合、登録画面から権限設定を開ける案内を追加
- アクセシビリティ要素の`unsafeBitCast`がInputMethodKitの切り替え後にメモリ破損を起こし得る問題を修正
- Core Foundationの型IDを検証してからARC管理下で変換する方式へ変更
- 入力ソースメニューへフォーカスが移った場合に備え、システム全体だけでなく最前面アプリのフォーカス要素も参照
- WebKitとChromium向けに選択テキストマーカー範囲から本文を取得する経路を追加
- Chromiumでは選択テキストマーカーがWeb領域配下の静的テキストへ付く構造を確認
- フォーカスウィンドウから子階層を最大2,000要素まで探索する取得経路を追加
- 選択文字列やURLを記録せず、権限状態、探索要素数、取得文字数だけを診断ログへ追加
- 状態確認画面へアクセシビリティの許可状態を追加

### 選択文字列登録のmacOSサービス化

- 入力ソースメニューからアクセシビリティ経由で選択範囲を取得する方式を廃止
- 右クリックの「サービス → myimのユーザー辞書に登録」から登録する方式へ変更
- macOSサービス用ペーストボードから選択文字列を直接受け取る構成へ変更
- サービス用ペーストボードにHTTPまたはHTTPS URLが含まれる場合だけ典拠として保存
- 登録後は実行中のInputControllerへ通知し、ユーザー辞書を即時再読み込み
- アクセシビリティ権限とブラウザ固有のアクセシビリティツリーへの依存を削除
- Input Methodsフォルダ内のサービスを右クリックメニューへ反映するため、インストール時にLaunch Servicesへ明示登録

## 2026-08-12

### 選択文字列登録の取りやめ

- 選択文字列をユーザー辞書へ登録する機能を一旦削除
- 入力ソースメニュー、アクセシビリティ探索、macOSサービスの各方式を削除
- 選択元URLの保存と辞書登録元ページを開くショートカットを削除
- 関連する典拠メタデータ、テスト、READMEの操作説明を削除
- Command＋VとOption＋Returnによる従来のユーザー辞書登録は維持
- 再インストール時に旧macOSサービスをLaunch Servicesから登録解除

### 候補生成処理のリファクタリング

- 候補の収集と順位付けを`CandidatePipeline`へ分離
- 辞書検索結果を完全一致と前方一致に分類する共通データ型を追加
- ユーザー辞書、拡張辞書、基本辞書、Mozc抽出辞書、補助辞書の重複検索を削減
- 完全一致と前方一致を一度の辞書走査で取得するよう変更
- 既存の候補順、履歴順位、7件単位の候補表示を維持
- 候補生成パイプラインと辞書検索分類の回帰テストを追加

### 入力状態管理のリファクタリング

- 通常入力、通常候補、もしかして、次入力候補、辞書登録を共通の入力状態として定義
- 辞書登録と曖昧候補のキー処理を共通状態判定へ接続
- 複数のOptional値を個別に調べていた状態判定を一か所へ集約
- IM終了時のパネル、タイマー、外部候補タスクの終了処理を共通化
- 状態の優先関係を回帰テストへ追加

### 履歴保存処理のリファクタリング

- 候補選択履歴と次入力履歴のJSON保存を入力処理から分離
- 連続する履歴更新を0.4秒単位でまとめてバックグラウンド保存
- メモリ上の候補順位と次入力候補は確定直後に更新する動作を維持
- IM終了時は未保存の履歴を同期的にフラッシュ
- 次入力履歴の削除時は保留中の古い保存を破棄して即時保存
- 遅延保存、最新値の統合、即時保存の回帰テストを追加

### 曖昧検索処理のリファクタリング

- 曖昧検索の辞書走査をキー入力の同期処理から分離
- 入力停止から80ミリ秒後にバックグラウンドで検索
- 入力が続いた場合は待機中の古い検索をキャンセル
- 確定、候補選択、辞書再構築、機能無効化で検索タスクを終了
- 遅れて完了した古い検索結果を現在の候補パネルへ反映しないよう制御
- 曖昧候補と意味候補が異なる順序で完了しても同じパネルへ統合
- 直接候補との競合除外処理をCoreへ分離して回帰テストを追加

### 候補検索セッションの共通化

- 曖昧検索、意味検索、外部候補のTaskと検索文字列を共通セッションへ統合
- 検索種別と検索文字列を持つトークンで最新の検索を判定
- 同じ検索種別を開始した場合は以前のTaskを自動的にキャンセル
- 検索種別ごとのキャンセルと全検索の一括終了を共通化
- 入力更新では外部候補を維持し、候補選択では補助候補だけを終了する動作を維持
- 古い検索トークン、検索種別の独立性、一括終了の回帰テストを追加

### メニューと設定画面の分離

- 入力ソースメニューの項目生成を`InputSourceMenuBuilder`へ分離
- 設定ウィンドウのレイアウトとコントロール生成を`SettingsWindowBuilder`へ分離
- 設定値とアクションを専用データとしてビルダーへ渡す構成へ変更
- 設定の読み書きと各アクション本体は`InputController`に維持
- 設定項目、表示順、ラベル、チェック状態、ショートカットを維持
- `InputController`から約140行のUI構築処理を削減

### 設定ダイアログの分離

- 日時書式、外部候補、外部情報パネルの入力UIを`SettingsDialogController`へ分離
- ダイアログは検証済みの設定値だけを`InputController`へ返す構成へ変更
- UserDefaults、認証ストア、候補更新の処理は`InputController`に維持
- 設定入力とCosense認証で使用するモーダル表示と貼り付け処理を共通化
- URLテンプレート検証、日時書式の重複除外、表示遅延の選択肢を維持
- `InputController`から約150行のダイアログ構築処理を削減

### Cosense設定の分離

- CosenseプロジェクトURLの入力と検証を`CosenseSettingsController`へ分離
- Personal Access TokenとService Accountの入力、貼り付け、保存、削除を分離
- 認証情報ストアへの読み書きを設定コントローラへ集約
- `InputController`は選択された設定の反映と辞書更新開始だけを担当
- プロジェクト単位のService Accountと共通Personal Access Tokenの動作を維持

### リファクタリング予定の整理

- 辞書の読み込み、更新、キャッシュ、変換エンジン再構築の共通管理を実装予定へ追加
- ユーザー辞書操作と外部候補検索の入力制御からの分離を追加
- パネル表示制御とUserDefaults設定アクセスの集約を追加
- 現在の機能と候補順を維持しながら段階的に移管する方針を明記

### コアと拡張機能の分離方針

- 入力、ローカル辞書検索、候補統合、選択、確定をコア機能として独立する予定を追加
- Cosense、外部候補、曖昧・意味検索、次入力予測、日時候補、外部情報表示を拡張機能へ分類
- 拡張機能の候補をコアの候補パイプラインで順位付けする内部APIの整備を追加
- 内部ProtocolとSwift Package Targetの分離を外部プラグイン化より先に実施する方針を追加
- 外部プラグイン方式は必要性と安全性を確認した機能だけ将来検討

### コア辞書設定の簡素化

- ローカル基本辞書とローカルユーザー辞書のオン・オフ設定を削除
- 基本辞書、Mozc抽出辞書、補助辞書、活用候補を常時使用する構成へ変更
- ユーザー辞書を通常候補と曖昧検索で常時使用する構成へ変更
- 任意のCosense拡張辞書と外部機能のオン・オフ設定は維持
- 過去に保存されたローカル辞書の無効設定は参照せず、コア機能を常に有効化

### 実装予定の分類

- READMEの実装予定を「機能追加」と「リファクタリング」に分割
- 利用者向けの改善と内部構造の整理を区別して確認できる構成へ変更
- 既存の実装予定は削除せず目的別に並べ替え

### 次入力候補の直前選択優先

- 同じ語句の後で最後に選ばれた次入力候補を先頭へ表示
- 2番目以降は従来の累計頻度、最終使用時点、文字列順を維持
- 候補履歴の上限整理でも直前候補を必ず保持
- 保存形式を変更せず既存の次入力履歴との互換性を維持
- 直前候補、残りの頻度順、履歴上限時の保持を回帰テストへ追加

### 誤入力補完の強化

- 利用者向けの「曖昧検索」という名称を「誤入力補完」へ変更
- 7文字以上のローマ字入力では最大2編集までの誤入力を検索
- 基本辞書の主要なて形を誤入力検索用の索引へ追加
- `hiruyou`から`必要`、`susmete`から`進めて`を「もしかして？」候補として生成
- 検索は入力停止後のバックグラウンド処理を維持
- 2編集の誤入力と活用形の誤入力を回帰テストへ追加

### Azure関連機能の削除

- Azure Translator Dictionary Lookupによる英訳候補を削除
- Azure APIキーとリージョンの設定項目を削除
- Azure通信クライアント、認証情報ストア、解析テストを削除
- 外部候補の取得をWikipediaとApple Translationだけに簡素化
- 外部候補設定からWeb検索先だけを設定する画面へ整理
- 旧バージョンが保存したAzure APIキーファイルはユーザーデータ保護のため自動削除しない

### 誤入力候補の表示フィルター修正

- 直接一致候補がある場合に距離2の誤入力候補を破棄していた条件を削除
- 文字数が異なる挿入・脱落候補を破棄していた条件を削除
- 通常候補パネルへ既に表示されている候補だけを「もしかして？」から除外
- `hiruyou`から`必要`と`susmete`から`進めて`が表示段階まで残るテストを追加

### 重み付き誤入力補完の実験実装

- `experiment/weighted-typo-correction`ブランチへ実験実装を分離
- SymSpell型の削除索引で編集距離の計算対象を事前に絞り込み
- 索引キーを64ビット化し、ハッシュ衝突は最終編集距離判定で除外
- QWERTY隣接キー、母音の脱落、転置を低コストにする重み付き編集距離を追加
- 重み付き誤入力コストと基本辞書順を組み合わせて候補を順位付け
- 不自然な2編集候補を重み付きコスト上限で除外
- 重い削除索引と活用索引の構築をバックグラウンドへ移動
- 同梱基本辞書全体で`hiruyou`から`必要`と`susmete`から`進めて`が上位3件へ入るテストを追加
- QWERTY配列を行のずれを含む座標として定義し、物理距離の近いキーから補正
- 隣接キー補正後の読みをMozc抽出辞書へ直接照合する経路を追加
- ローマ字基本辞書にない`vaiyou`から`baiyou`を経由して`培養`を表示

### 実装予定の棚卸し

- 実装済みの誤入力補完の速度、メモリ、候補精度改善をREADMEの実装予定から削除
- 意味検索の改善を独立した実装予定として維持
- コア分離、候補パイプライン、Swift Package Target分離を残作業に限定した表現へ更新
- ユーザー辞書操作など部分的に分離済みの項目は未完了部分を引き続き記載

### 意味検索の隣接キー補正

- 意味検索の読み分割をOS非依存の`MyIMECore`へ移動
- 元の読みとQWERTY隣接キー補正後の読みを比較
- 短い断片の寄せ集めより長い辞書読みを優先
- `tatemomowosekkeisuruhito`を`tatemono`、`sekkei`、`hito`へ分割して意味検索へ渡すテストを追加

### 意味検索機能の削除

- 意味検索は期待する候補を安定して生成できないため機能全体を削除
- 設定、検索処理、意味辞書、ベクトル索引、生成スクリプト、テストを削除
- Natural Language frameworkへのリンクと起動時のベクトル索引読込を削除
- 「もしかして？」パネルを誤入力補完専用へ簡素化
- 今後は誤入力補完と入力揺れの吸収を優先して改善

### 誤入力補完の調査と選択履歴

- 増井俊之氏の`asearch`とBitapによる挿入、削除、置換のビット並列判定を調査
- 辞書全走査を避ける現在の削除索引を候補抽出に維持
- Damerau-Levenshtein距離とローマ字向け重み付きコストを候補検証に維持
- Bitapは将来の前方一致とインクリメンタルな絞り込みへの適用候補として整理
- 「もしかして？」候補にも選択履歴を適用し、直近に選択した補助候補を優先
- 履歴のない補助候補は従来の誤入力コストと辞書順を維持

### 候補選択キャンセル後の誤入力候補

- 通常候補の選択開始時に誤入力検索を停止する動作は維持
- Escapeで通常候補の選択を解除した場合に候補生成を再実行
- 入力中のローマ字表示へ戻した後に「もしかして？」候補を再表示

### 辞書登録モードの表示

- 辞書登録中の候補パネルへアクセント色の枠を追加
- パネル上部へ「ユーザー辞書登録」の見出しを追加
- 語句入力中と登録確認の段階を見出しで区別
- 既存の読み、登録語句、ショートカット案内を維持

### 候補パネルの案内整理

- 通常候補パネルから「Shift＋Tab もしかして？」の案内を削除
- 「もしかして？」パネル内の選択ショートカット案内は維持
- 通常候補パネルの候補選択、辞書登録、文字種変換、外部ページの案内は維持

### 辞書登録中の変換候補表示

- 辞書登録中に表示する変換候補パネルにもアクセント色の枠を適用
- 変換候補パネルへ「ユーザー辞書登録 · 変換候補」の見出しを表示
- 通常の確定やWeb検索ではなく、登録語句への追加を示す操作案内へ変更
- 語句入力、変換候補、確認の表示を同じ登録モードの外観へ統一

### 辞書登録モードの表示簡素化

- 登録モードの見出しを「登録したい文字列を入力」へ統一
- 語句入力、変換候補、確認の段階名を削除
- パネル内の重複した状態説明と操作説明を削除
- 読み、入力内容、下部のキー操作案内だけを維持

### 辞書登録と候補移動のキー整理

- 通常入力中のCommand＋Vによる即時辞書登録を削除
- 辞書登録の開始操作をOption＋Returnへ一本化
- 辞書登録モード内のCommand＋Vは登録文字列の貼り付けとして維持
- 次入力候補パネルをSpaceで維持する動作と案内を削除
- Spaceは候補移動に使わず、入力または選択候補を確定して半角空白を入力
- 候補移動をTab、Shift＋Tab、矢印キーへ統一

## 2026-08-13

### 入力中のカーソル編集

- 候補未選択中の左右矢印をローマ字内のカーソル移動へ変更
- カーソル位置への文字挿入とDeleteに対応
- 候補選択の開始をTabだけに限定
- 候補選択後は従来どおり上下左右の矢印で候補を移動
- 候補未選択中の上下矢印は入力先アプリへ渡す
- 辞書登録モード内のローマ字入力にも同じカーソル編集を適用

### 未確定入力のカーソル表示

- 入力先アプリが未確定文字列の標準キャレットを描画しない場合へ対応
- myim側で現在位置へアクセント色の細いカーソルを表示
- 左右移動、途中挿入、Deleteのたびに表示位置を更新
- 候補選択中、確定後、入力終了時はmyim側のカーソルを非表示

## 2026-08-14

### 未確定入力の標準カーソルへ復帰

- アプリ間で正確な位置を取得できない独自カーソルウィンドウを削除
- `selectionRange`を入力先アプリへ通知するmacOS標準方式へ復帰
- 左右移動、途中挿入、Deleteによる未確定文字列の編集は維持

### 選択文字列の辞書登録

- 入力先で文字列を選択してOption＋Returnを押す登録方法を追加
- 選択文字列を変換先として保持し、登録パネル内でローマ字の読みを入力
- 登録中の読みは入力先へ未確定文字列として送らず、選択本文を維持
- Returnでユーザー辞書へ保存し、Escapeで変更せず中止
- アクセシビリティ、クリップボード、macOSサービスへの依存を追加しない構成を採用
- 選択文字列を同じ内容の未確定文字列として保持し、登録完了のReturnを変換確定として消費
- Webエディタへ登録完了のReturnが渡り、改行される問題へ対応

### 選択文字列登録の再削除

- 文字列選択後のOption＋Returnによる辞書登録を削除
- 入力欄以外ではInputMethodKitへショートカットが届かず、すべての選択可能な文字列を対象にできないため不採用
- ローマ字入力中のOption＋Returnによる通常の辞書登録は維持

### 計算候補

- `2+1=`のような入力から計算結果を変換候補として表示
- 四則演算、小数、括弧、単項の正負記号へ対応
- 外部APIや動的コード実行を使わないローカルの式パーサーを追加
- 0除算、不正文字、式の途中、極端に長い式では候補を表示しない

### READMEの便利機能整理

- 日付、時刻、計算、文字種変換を「便利機能」へ集約
- 基本的な記号変換と数字表記はREADMEの機能紹介から省略
- ローカル変換の説明を辞書と候補生成の役割へ限定
- 常時利用できる機能と設定で有効にする日時候補を同じ場所で区別

### ユーザー辞書候補の削除範囲

- 読みの完全一致を必要とせず、選択中の候補をユーザー辞書全体から削除
- `rea`の前方一致候補から`readme`へ登録した`README`を削除できるよう変更
- 同じ出力が複数の読みに登録されている場合はすべてのユーザー登録を削除
- Mozc、TKGJE、Cosenseなどユーザー辞書以外の候補は変更しない

### 選択文字列の再変換

- 入力可能な場所で確定済み文字列を選択し、Tabで再変換を開始する操作を追加
- ユーザー辞書、Cosense、TKGJE、myim補完辞書、Mozcから読みを逆引き
- Mozcのかな読みをmacOS標準のローマ字変換でmyim入力へ復元
- Returnで選択範囲を候補へ置換し、Escapeやフォーカス移動では元の文字列へ復元
- Tabがmyimへ届かない閲覧専用領域は対象外

### 翻訳モード

- 入力ソースメニューと設定へ「翻訳モード」を追加
- Option＋Tで翻訳モードを切り替える操作を追加
- 切り替え時に入力位置付近へオン・オフ状態を示す通知パネルを追加
- 変換した日本語を本文へ確定せず、翻訳用の未確定文字列へ追加
- 辞書登録モードと同様に、保持中の日本語とReturn、Escapeの操作を専用パネルへ表示
- 複数回に分けて変換した日本語を同じ未確定範囲で連結
- 入力が空の状態でReturnを押すと文章翻訳を開始し、英語だけを本文へ確定
- 翻訳実行のReturnを入力先へ送らず、翻訳待機中も日本語の未確定表示を維持
- Escapeでは翻訳を中止し、保持中の日本語をそのまま確定
- 確定済み本文の位置追跡と後からの範囲置換を廃止
- 通常の単語翻訳候補から先頭の`a` `an` `the`と末尾のピリオドを除去
- macOS 15未満では翻訳モードを有効化しない

### 候補パネルの操作案内

- 案内文の実測幅に合わせて候補パネルを拡張
- 画面幅に収まらない案内文は行数制限なしで折り返し
- 日本語や空白の少ない案内も文字単位で複数行へ折り返し
- 「もしかして？」パネルの操作案内も複数行表示へ統一
- AppKitのテキストセルへ複数行、折り返し、非切り詰めを明示
- 長い案内は操作のまとまりごとに改行して見切れを防止
- 案内文の予測計算と実描画が異なる構造を廃止
- NSTextViewのレイアウトマネージャーが算出した実描画高をパネルへ反映
- 翻訳モード中の候補パネルへOption＋Tの終了操作を表示

### Space確定後の次入力候補

- Spaceで入力または選択候補と半角空白を従来どおり確定
- 履歴には空白を除いた語句本体を記録して次入力候補を検索
- Space入力後も次入力候補パネルを維持
- 通常の変換候補パネルを残す変更は意図と異なるため撤回
- Escapeで次入力候補の選択だけを解除し、パネルと自動終了を継続
- InputMethodKitが先に選択を解除する場合を考慮し、未選択状態のEscapeでもパネルを維持

### 入力ソースの再登録

- インストーラーが有効な入力ソースだけを検索し、無効化されたmyimを発見できない問題を修正
- 登録済みと無効状態を含む全入力ソースからmyimを検索
- myimを明示的に有効化してから選択
- 登録後にmyimが見つからない場合を成功扱いせず、インストールエラーとして報告

## 2026-08-17

### GitHub Release向けPKG配布

- Apple SiliconとIntelに対応するUniversal Binaryの配布ビルドを追加
- Developer ID ApplicationとDeveloper ID Installerによる署名へ対応
- Apple公証、Staple、Gatekeeper、PKG署名の検査処理を追加
- バージョンタグからPKGとSHA-256をGitHub Releaseへ公開するActionsを追加
- 利用者向けインストール手順とリリース担当者向けSecrets設定手順を追加
- PKGによる既存アプリの再配置を禁止し、更新時に実行中のmyimを終了する処理を追加
- PKGから不要なAppleDoubleファイルを除外し、内包ブラウザも同じ場所で更新するよう固定
- mainとPull RequestでテストとUniversal app生成を検査するCIを追加
- 過去コミットの再ビルドではHISTORYの日付を改変せず、未来の日付だけを拒否する検査モードを追加

## 2026-08-23

### 新しいmacOSでのクラッシュ耐性

- クラッシュログ5件で共通していたInputMethodKitのセッション解除競合を調査
- 独自候補パネルへ移行後も残っていた未使用の`IMKCandidates`を撤去
- macOS内蔵辞書一覧の要素を強制キャストせず、不明な型を安全に読み飛ばすよう修正
- 候補パネルのテキストレイアウトで強制アンラップを廃止
- 別端末での原因特定に使うクラッシュログ採取手順をREADMEへ追加

### GitHubワークフローの削除

- CIとリリース用のGitHub Actionsワークフローを削除
- 署名済みPKGの作成と公証手順をローカル実行へ統一

### Tabによる入力欄移動

- 次入力候補をEscapeで閉じても入力欄のフォーカスを維持
- Escapeで閉じた後のTabをアプリへ渡して入力欄を移動可能に修正
- 次入力候補と選択文字列再変換の既存Tab操作は維持

### 濁音候補の誤推薦抑制

- 誤入力補完で清音子音から濁音子音への置換コストを引き上げ
- 隣接キー補完から濁点を追加するだけの候補を除外
- 濁音同士のタイプミス補完と連濁の補助検索は維持

### 外来語のかな変換

- `sajesuto`でひらがな・カタカナ候補を生成できない原因を修正
- シェ、ジェ、チェ、クァ、グァ、トゥなどの拡張ローマ字を追加
- 解釈できない音節が含まれる場合に候補を生成しない既存仕様は維持

### 通常候補の文脈最適化

- 次入力履歴を通常候補の順位付けにも利用
- 直前の確定語に続いて使用した候補を単体の選択履歴より優先
- 一文字かな優先、履歴上限、辞書にない候補を追加しない仕様を維持

### 全角スペース入力

- Shift＋Spaceで全角スペースを入力する操作を追加
- 入力中、候補選択中、翻訳モードでも全角スペースを使用可能
- 通常のSpaceによる半角スペース入力は維持

## 2026-08-23

### インスピレーション元

- README冒頭のインスピレーション元へPOBoxを追加

### 辞書表記に合わせたかな候補

- 辞書の完全一致候補がカタカナの場合はカタカナをひらがなより優先
- 辞書の完全一致候補がひらがなの場合はひらがなをカタカナより優先
- 両方ある場合は辞書順、どちらもない場合は従来のひらがな優先を維持

### 辞書input形式の統一

- 辞書の基本単位を`input → candidates`として再定義
- 生入力の完全一致を優先し、日本語ローマ字として解析できる入力だけを正規化して追加検索
- `si / shi`などの正しい表記差と誤入力補完を分離
- Mozc由来辞書のかな読みを正規ローマ字inputへ変換
- Mozc辞書を280,770かな読み、383,519候補、約9.9MBから279,262 input、382,193候補、約8.0MBへ整理
- Mozc検索前のローマ字からかなへの変換を削除
- `ime-dictionary`を`mozc-dictionary`へ改名
- 表記正規化後も文字入れ替えと隣接キーの誤入力補完を維持
- `kakujuu → 拡充`がMozcに存在することを確認し、コード内の重複補完辞書と専用検索レイヤーを削除

## 更新方針

- 実装または仕様が変更された時点でこのファイルを更新
- READMEには現在の利用方法と仕様を記載
- HISTORYには変更の時系列と設計上の転換を記載
- 日付見出しは`Scripts/verify-history-date.sh`で日本時間の当日を検証
